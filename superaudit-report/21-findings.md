# 21. Registro central de hallazgos

> Auditor: report-consolidator | Fecha: 2026-04-22

## Resumen

* **Total hallazgos:** 61
* **Criticos:** 9
* **Altos:** 21
* **Medios:** 22
* **Bajos:** 9
* **Reportes analizados:** 13 de 14
* **Reportes faltantes:** `06-design-accessibility.md` (Design System + Accesibilidad — no auditado)

---

## Registro completo

| ID | Dominio | Subzona | Hallazgo | Severidad | Impacto | Evidencia | Accion | Estado |
|----|---------|---------|----------|-----------|---------|-----------|--------|--------|
| SEG-001 | Seguridad | Cifrado de licencias | Clave AES-256-GCM hardcodeada en binario Swift de BrewBar: hex `5c3b2ae2a3066bca28773f36db347d8c8a0a396d4b9fab628331446acd6d4126` compilado y extraible con `strings`. Adicionalmente, `ENCRYPTION_SECRET = 'brew-tui-license-aes256gcm-v1'` y `SCRYPT_SALT = 'brew-tui-salt-v1'` en texto claro en el bundle npm publico con source maps incluidos. | Critica | Cualquier persona puede descifrar cualquier `~/.brew-tui/license.json` y forjar licencias Pro; el modelo de proteccion queda comprometido por completo | `LicenseChecker.swift:47`; `src/lib/license/license-manager.ts:60-61`; `tsup.config.ts:12` (source maps) | Redisenar el sistema de licencias con tokens opacos firmados con ECDSA server-side. A corto plazo: deshabilitar source maps en el bundle publico (`sourcemap: false` en tsup) y rotar el secreto | Pendiente |
| SEG-002 | Seguridad | Anti-debug | Bypass trivial del sistema anti-debug: `if (process.env.VITEST \|\| process.env.CI) return false` permite ejecutar `VITEST=1 brew-tui` y desactivar todas las protecciones. | Critica | La capa de proteccion anti-debug es completamente ineficaz para cualquier usuario que conozca esta variable de entorno | `src/lib/license/anti-debug.ts:11` | Eliminar el bypass por variable de entorno; usar un flag de compilacion inyectado por tsup (`define: { '__DEBUG_EXEMPT__': 'false' }`) | Pendiente |
| SEG-003 | Seguridad | Distribucion BrewBar | BrewBar se distribuye sin firma de codigo (Developer ID), sin notarizacion de Apple, y sin App Sandbox. macOS Gatekeeper bloqueara la app en todos los usuarios que la descarguen desde GitHub Releases o el cask de Homebrew. | Critica | La aplicacion es inutilizable para la mayoria de usuarios sin ejecutar `xattr -cr BrewBar.app`; ademas la ausencia de App Sandbox expone al usuario a riesgos de seguridad | `.github/workflows/release.yml:57-69` — sin `codesign` ni `xcrun notarytool`; ausencia de archivo `.entitlements` | Configurar certificado Developer ID en CI via GitHub Secrets; agregar pasos de `codesign --deep --force --options runtime` y `xcrun notarytool submit`; crear `BrewBar.entitlements` con `com.apple.security.app-sandbox = true` | Pendiente |
| SEG-004 | Seguridad | Integridad de descarga | `brewbar-installer.ts` descarga e instala `BrewBar.app.zip` desde GitHub sin verificar checksum SHA-256 ni firma de codigo, y sin limite de tamano en la descarga. | Critica | Vector de ataque MITM o sustitucion de binario; una descarga corrompida o maliciosa se instala directamente en `/Applications/` | `src/lib/brewbar-installer.ts:42-60` | Publicar checksum SHA-256 junto al release; verificar hash post-descarga con `crypto.createHash`; verificar firma con `codesign --verify`; comprobar `Content-Length` y abortar si supera umbral (ej. 200 MB) | Pendiente |
| SEG-005 | Seguridad | Desactivacion de licencia | La funcion `deactivate` en Polar API silencia errores con `catch { /* best effort */ }` y borra la licencia local incondicionalmente. Si la llamada API falla, el slot de activacion queda activo en Polar pero la licencia se borra localmente: el usuario pierde su licencia y no puede reactivar en otro equipo. | Critica | Perdida de acceso Pro del usuario sin posibilidad de recuperacion automatica; impacto directo en ingresos y soporte | `src/lib/license/license-manager.ts:257-261`; `src/lib/license/polar-api.ts:129-135` | Reintentar la llamada 2-3 veces antes de silenciar el error; advertir al usuario si la desactivacion remota fallo; guardar `instanceId` y `key` hasta confirmar exito remoto | Pendiente |
| QA-001 | Testing | Cobertura total | Cobertura de tests del 0% en ambos codebases. El codebase TypeScript tiene vitest y ink-testing-library instalados pero cero archivos de tests escritos. El codebase Swift tiene el scheme Xcode configurado pero sin ningun XCTest target. `npm run test` no se ejecuta en ningun workflow de CI. | Critica | Las operaciones destructivas (brew uninstall, force-uninstall, upgrade masivo, activacion de licencia de pago) se ejecutan sin ninguna red de seguridad automatizada; regresiones pasan a produccion sin deteccion | Ausencia de `*.test.ts`, `*.spec.ts` en `src/`; ausencia de `*.xctest` en `menubar/`; `release.yml` sin paso `npm run test` | Crear suite de tests unitarios empezando por `license-manager.ts` (degradacion, cifrado, rate limiting), parsers JSON, `profile-manager.ts`; escribir tests de componentes con `ink-testing-library`; agregar `npm run test` como gate en CI antes de `npm publish` | Pendiente |
| BK-001 | Backend | Timeouts en fetch | Ninguna de las siete llamadas `fetch()` en el codebase TypeScript define `signal: AbortSignal.timeout(...)`. Sitios afectados: `polar-api.ts` (activate, validate, deactivate), `osv-api.ts` (queryBatch, queryOneByOne), `brewbar-installer.ts` (descarga ZIP), y `execBrew()` en `brew-cli.ts` (procesos hijo sin timeout). | Critica | Una conexion colgada o un servidor externo que no responda bloquea el event loop indefinidamente; la UI queda congelada sin posibilidad de cancelacion | `src/lib/license/polar-api.ts:44`; `src/lib/security/osv-api.ts:64`; `src/lib/brewbar-installer.ts:42`; `src/lib/brew-cli.ts:4-21` | Crear helper `fetchWithTimeout(url, options, ms)` que use `AbortSignal.timeout(ms)` y aplicarlo en todos los sitios; valores: 15s para Polar/OSV, 120s para descarga BrewBar; agregar timeout de 60s a `execBrew` | Pendiente |
| GOV-001 | Gobierno | Licencia del proyecto | La licencia MIT permite a cualquier persona copiar, modificar y redistribuir el codigo fuente sin restricciones, incluyendo el codigo del sistema de proteccion Pro en `src/lib/license/`. | Critica | El codigo fuente del modelo freemium es libremente redistribuible; cualquier usuario puede eliminar las verificaciones de licencia y redistribuir | `LICENSE:1` — licencia MIT; `src/lib/license/` incluido en el paquete npm publico | Cambiar a Commons Clause + MIT o Business Source License 1.1 para la capa Pro; o mover el codigo Pro-gate a un modulo privado no incluido en el paquete npm publico | Pendiente |
| REL-001 | Release | PrivacyInfo.xcprivacy | El archivo `PrivacyInfo.xcprivacy` esta completamente ausente del proyecto. BrewBar usa `UserDefaults`, `FileManager` (timestamps) y `UNUserNotificationCenter`, todos los cuales requieren declaracion de razon de acceso desde macOS 14 / primavera 2024 de Apple. | Critica | Incumplimiento de requisito de Apple; la notarizacion en macOS 15+ puede fallar; riesgo de rechazo si se distribuye por App Store en el futuro | Ausencia del archivo en todo el repositorio; `SchedulerService.swift:44` usa `UserDefaults.standard` | Crear `menubar/BrewBar/Resources/PrivacyInfo.xcprivacy` declarando `NSPrivacyAccessedAPICategoryUserDefaults` con razon `CA92.1` y acceso a timestamps de ficheros con razon `C617.1` | Pendiente |
| GOV-002 | Gobierno | CI/CD publicacion duplicada | `release.yml` (trigger: tag `v*`) crea un GitHub Release que a su vez activa `publish.yml` (trigger: `on release: published`), resultando en dos intentos de publicacion npm para cada release. El segundo falla con 409 Conflict. | Alta | Cada release falla parcialmente en CI; riesgo de publicar una version incorrecta si el segundo intento tiene diferente contenido | `release.yml:3-6` trigger tag + `publish.yml:3-5` trigger release published | Eliminar `publish.yml` completamente (sus responsabilidades estan cubiertas por `release.yml`) | Pendiente |
| GOV-003 | Gobierno | Source maps en produccion | `tsup.config.ts:12` tiene `sourcemap: true` sin distincion por entorno; el bundle npm publicado incluye archivos `.js.map` que exponen la estructura interna del codigo, incluida la logica de licencias Pro | Alta | Cualquier usuario puede reconstruir el codigo fuente desde el bundle npm instalado, facilitando la extraccion de secretos y el bypass de protecciones | `tsup.config.ts:12` — `sourcemap: true` | Cambiar a `sourcemap: false` para el build publico; mantener source maps solo en entorno de desarrollo local | Pendiente |
| GOV-004 | Gobierno | Firma de BrewBar en CI | El workflow `release.yml` construye `BrewBar.app.zip` con `CODE_SIGN_IDENTITY = "-"` (ad-hoc) sin configurar firma Developer ID ni notarizacion. | Alta | Ver SEG-003 para el impacto en usuarios; ademas el artefacto puede ser sustituido en la cadena de suministro | `.github/workflows/release.yml:48-74` — job `build-brewbar` sin firma | Configurar `CODE_SIGN_IDENTITY` con certificado Apple Developer ID en GitHub Secrets; agregar `codesign` y `xcrun notarytool` en el job | Pendiente |
| ARQ-001 | Arquitectura | Inversion de dependencias | Los modulos `pro-guard.ts`, `anti-tamper.ts` y `watermark.ts` de la capa `lib/` importan directamente `stores/license-store` (capa de presentacion), invirtiendo la dependencia esperada. | Alta | Acoplamiento ciclico logico entre capa de dominio y capa de estado; dificulta los tests unitarios del sistema de licencias | `src/lib/license/pro-guard.ts:1`; `src/lib/license/anti-tamper.ts:1`; `src/lib/license/watermark.ts:1` | Refactorizar `verifyPro()` para aceptar el estado de licencia como parametro en lugar de acceder al store directamente | Pendiente |
| ARQ-002 | Arquitectura | Capa de vistas bypassa store | `OutdatedView` importa `execBrew` directamente para la accion pin/unpin, saltandose el store y la capa API. `ProfilesView` llama `profile-manager` directamente para la operacion de import. | Alta | Inconsistencia arquitectural; la logica de error no se captura en el mapa de errores del store; dificulta los tests | `src/views/outdated.tsx:5,54`; `src/views/profiles.tsx:12` | Agregar `togglePinPackage(name)` en `brew-api.ts` y `brew-store.ts`; mover la logica de import streaming al store | Pendiente |
| CON-001 | Concurrencia | setInterval sin limpieza | `setInterval` en `license-store.ts:71` no guarda la referencia y no cancela el interval previo. Si `initialize()` se llama multiples veces (HMR, tests, Strict Mode), se acumulan intervals que disparan revalidaciones duplicadas. | Alta | Multiples revalidaciones concurrentes de licencia; problemas en tests que deben resetear el modulo entre suites | `src/stores/license-store.ts:71` — `setInterval(async () => { ... }, REVALIDATION_CHECK_MS).unref()` sin referencia | Guardar el ID del interval: `let _intervalId: ReturnType<typeof setInterval> \| null = null`; cancelar el anterior antes de crear uno nuevo | Pendiente |
| CON-002 | Concurrencia | Task fire-and-forget en BrewBar | Multiples `Task { }` fire-and-forget en vistas SwiftUI sin handle de cancelacion. Las tasks no estan ligadas al ciclo de vida de la vista y pueden modificar `AppState` huerfano si la vista se desmonta. | Alta | Posibles actualizaciones de estado post-desmonte; comportamiento impredecible en navegacion rapida | `PopoverView.swift:54`; `OutdatedListView.swift:14` — `Task { await appState.upgrade... }` sin `.task {}` modifier | Capturar el handle en `@State private var task: Task<Void, Never>?` y cancelarlo en `.onDisappear`; alternativamente usar el modifier `.task { ... }` que cancela automaticamente | Pendiente |
| UI-001 | UI | goBack() inutilizable | `goBack()` intercambia `currentView` y `previousView` en lugar de hacer pop de `viewHistory`. El historial acumula hasta 20 entradas pero nunca se consume. Esc en C→B→Esc vuelve a C en lugar de a A. | Alta | El usuario no puede navegar mas de un nivel hacia atras; queda atrapado en ping-pong entre dos vistas | `src/stores/navigation-store.ts:35-42` | Reimplementar `goBack()` como pop de `viewHistory`: `const prev = viewHistory.at(-1); set({ currentView: prev, viewHistory: viewHistory.slice(0,-1) })` | Pendiente |
| UI-002 | UI | SearchView silencia errores | El catch en `doSearch` hace `void err` y establece resultados vacios. Errores de red, brew no instalado, o proceso fallido son indistinguibles de "sin resultados". | Alta | El usuario no puede distinguir entre "no hay paquetes" y "fallo de busqueda"; no puede tomar ninguna accion correctiva | `src/views/search.tsx:44-48` | Capturar el error en estado `searchError: string \| null` y renderizar `<ErrorMessage>` cuando no sea null | Pendiente |
| UI-003 | UI | MAX_VISIBLE_ROWS hardcodeado | `MAX_VISIBLE_ROWS = 20` hardcodeado en `installed.tsx`, `history.tsx` y `outdated.tsx`. En terminales de menos de 22 filas el contenido desborda o queda cortado. OutdatedView ademas no implementa ninguna virtualizacion. | Alta | Layout roto en terminales pequenas; en OutdatedView con 50+ paquetes desactualizados todos los elementos se renderizan sin limite | `src/views/installed.tsx:15`; `src/views/history.tsx:18`; `src/views/outdated.tsx:13,126` | Usar `useStdout()` de Ink para `stdout.rows` dinamico: `const maxRows = Math.max(5, (stdout?.rows ?? 24) - 8)`; aplicar virtualizacion en OutdatedView | Pendiente |
| UI-004 | UI | GradientText sin memoizacion | `GradientText` crea un nodo `<Text>` por caracter. El logo ASCII genera ~258 nodos React en cada render del Header (que ocurre en cada cambio de navegacion). | Alta | Re-renders costosos en cada navegacion; degradacion de rendimiento perceptible en terminales lentas o con muchos paquetes | `src/utils/gradient.tsx` — `chars.map((char, i) => <Text key={i} color={...}>{char}</Text>)` | Memoizar con `React.memo` y `useMemo`; o pre-renderizar el logo como string ANSI estatico con chalk usando un solo `<Text>` | Pendiente |
| UI-005 | UI | NSAppleScript hardcodea Terminal | `openBrewTUI()` en BrewBar usa `tell application "Terminal" to do script "brew-tui"` hardcodeado. Falla silenciosamente (solo NSLog) para usuarios de iTerm2, Warp, Ghostty, Alacritty, Kitty. | Alta | La funcionalidad principal de "abrir Brew-TUI" del menubar es inutilizable para una fraccion significativa de usuarios | `menubar/BrewBar/Sources/Views/PopoverView.swift:176` | Ofrecer picker de terminal en SettingsView; alternativamente usar `NSWorkspace` o `Process` para lanzar `brew-tui` directamente sin NSAppleScript | Pendiente |
| UI-006 | UI | Cero tests de componentes UI | `ink-testing-library` instalada en devDependencies pero sin ningun archivo `.test.tsx` en `src/`. Ningun componente comun ni vista tiene tests de renderizado o interaccion. | Alta | Las regresiones en flujos destructivos (uninstall, upgrade, confirm dialog) pasan a produccion sin deteccion | `package.json` — `"ink-testing-library": "^4.0.0"`; ausencia de `*.test.tsx` en `src/` | Crear tests para: `<ConfirmDialog>` (confirm/cancel en ambos idiomas), `AccountView` (estados free/pro/expired), `UpgradePrompt` (renderizado por viewId), guard Pro en `app.tsx` | Pendiente |
| UX-001 | UX | ProfilesView modo importing bloqueado | En modo `importing`, `openModal()` suprime el teclado global y ningun `useInput` tiene `isActive`. Al completar el import, el texto "Press any key" es falso: ninguna tecla funciona. El usuario queda atrapado hasta descubrir que Tab funciona. | Alta | El usuario queda bloqueado en pantalla sin posibilidad de escape visible; experiencia de usuario deficiente en una operacion critica Pro | `src/views/profiles.tsx:46-53, 112-124` | Agregar `useInput` activo cuando `mode === 'importing'` que en Esc o cualquier tecla llame `setMode('list')` al completar | Pendiente |
| UX-002 | UX | InstalledView bloqueada post-desinstalacion | Tras una desinstalacion, la vista muestra `ProgressLog` permanentemente sin tecla de escape ni hint visible. No hay `stream.clear()` equivalente al que tienen `outdated.tsx` y `smart-cleanup.tsx`. | Alta | El usuario queda atrapado viendo el log de salida; la lista de paquetes es inaccesible hasta navegar a otra vista | `src/views/installed.tsx:95-113` — ausencia de handler de Esc y `stream.clear()` | Agregar hint `esc:back` y llamar `stream.clear()` en Esc, igual que en `outdated.tsx` | Pendiente |
| UX-003 | UX | Cursor de busqueda desincronizado | En SearchView, `allResults` contiene todos los resultados pero el render limita a 20 por seccion. Si hay mas de 20 formulae, el cursor puede apuntar a indices 20-39 con `allResults[cursor]` apuntando a formulae no renderizadas; la seleccion visual `▶` apunta a una fila incorrecta. | Alta | El usuario selecciona elementos que no corresponden visualmente a lo que intenta instalar; comportamiento impredecible potencialmente instalando el paquete equivocado | `src/views/search.tsx:61, 159-170, 177-188` | Limitar `allResults` al mismo slice visible o desactivar el cursor para posiciones fuera del rango renderizado | Pendiente |
| UX-004 | UX | BrewBar Upgrade All sin confirmacion | `OutdatedListView.swift` ejecuta `upgradeAll()` directamente desde el boton sin ningun dialogo de confirmacion. Tambien el upgrade individual por paquete se ejecuta sin confirmacion. | Alta | Actualizaciones masivas accidentales; inconsistencia con el TUI que requiere confirmacion para todas las operaciones destructivas | `menubar/BrewBar/Sources/Views/OutdatedListView.swift:13-15` | Agregar `NSAlert` de confirmacion antes de ejecutar el upgrade masivo; agregar confirmacion tambien para upgrades individuales | Pendiente |
| UX-005 | UX | BrewBar licencia vencida termina la app | `AppDelegate` muestra NSAlert y llama `NSApp.terminate(nil)` cuando la licencia esta vencida o ausente, sin ofrecer ninguna opcion de activacion desde la propia app. | Alta | El usuario pierde acceso a la herramienta abruptamente sin guia de recuperacion; experiencia catastrofica para usuarios con licencia expirada | `menubar/BrewBar/Sources/App/AppDelegate.swift:26-32` | Mostrar UI de activacion inline con instrucciones o enlace a polar.sh; no terminar la app forzosamente | Pendiente |
| BK-002 | Backend | Permisos de archivos de datos | `history.json` y perfiles se crean sin modo de archivo explicito (heredan la umask del proceso, tipicamente `0o644`). `ensureDataDirs()` crea directorios sin `mode` explicito (resulta en `0o755`). Solo `license.json` tiene `mode: 0o600`. | Alta | Historial de operaciones (install/uninstall/upgrade) y perfiles Homebrew del usuario son legibles por cualquier otro usuario del sistema | `src/lib/history/history-logger.ts:25`; `src/lib/profiles/profile-manager.ts:67`; `src/lib/data-dir.ts:11-12` | Pasar `{ mode: 0o600 }` en ambas llamadas `writeFile`; agregar `{ recursive: true, mode: 0o700 }` en `mkdir` | Pendiente |
| BK-003 | Backend | Escritura de license.json no atomica | `license-manager.ts` usa `writeFile` directo para `license.json`, mientras `history-logger.ts` ya implementa escritura atomica via tmp+rename. Si BrewBar lee el archivo mientras TypeScript escribe, puede obtener JSON corrupto parcial. | Media | Posible corrupcion del archivo de licencia ante escrituras concurrentes TS/BrewBar; el usuario puede perder acceso Pro | `src/lib/license/license-manager.ts:133` vs `src/lib/history/history-logger.ts:24-26` (patron correcto) | Reemplazar `writeFile(LICENSE_PATH, ...)` por el patron tmp+rename ya implementado en `history-logger.ts` | Pendiente |
| BK-004 | Backend | Rate limiting no persiste entre reinicios | El `tracker` de rate limiting en `license-manager.ts` es un objeto en memoria. Al reiniciar el proceso, los contadores se resetean, anulando el lockout de intentos fallidos de activacion. | Alta | Un atacante puede realizar ataques de fuerza bruta sobre el sistema de activacion reiniciando el proceso entre intentos | `src/lib/license/license-manager.ts:22-26` — `tracker` como objeto de modulo | Persistir el estado del lockout en `~/.brew-tui/license-ratelimit.json` con permisos `0o600` | Pendiente |
| BK-005 | Backend | Validacion de schema JSON local ausente | `history-logger.ts` y `profile-manager.ts` castean `JSON.parse(raw)` directamente a tipos TypeScript sin validar la estructura. Un archivo corrupto o modificado manualmente puede causar errores en runtime no anticipados. | Media | Crashes o comportamiento impredecible al cargar archivos de datos del usuario corrupto o con formato de version futura | `src/lib/history/history-logger.ts:13`; `src/lib/profiles/profile-manager.ts:52` | Agregar validacion de estructura: `Array.isArray(file.entries)`, `typeof entry.id === 'string'`; considerar una funcion `parseHistoryFile(raw)` con validacion explicita | Pendiente |
| BK-006 | Backend | Sin migracion de schema | Los tres formatos de archivo (`license.json`, `history.json`, perfiles) tienen campo `version: 1` pero no existe logica de migracion para ninguna version futura. Un cambio de esquema invalidara silenciosamente todos los datos de usuarios existentes. | Media | Imposibilidad de evolucion del formato de datos sin romper instalaciones existentes de usuarios; deuda tecnica critica a largo plazo | `src/lib/history/history-logger.ts:23`; `src/lib/profiles/profile-manager.ts:66`; campo `version` escrito pero nunca leido para ramificar logica | Implementar `switch (file.version)` en cada funcion de carga; documentar el proceso de migracion | Pendiente |
| SEG-006 | Seguridad | Validacion de paquetes en perfiles importados | Los arrays `profile.taps`, `profile.formulae`, `profile.casks` de un JSON importado se pasan directamente a `brew tap` y `brew install` sin validar el formato Homebrew. | Alta | Un perfil JSON malicioso podria contener nombres de paquetes o taps crafteados para explotar vulnerabilidades en brew o en el sistema | `src/lib/profiles/profile-manager.ts:130-151` | Validar que taps sigan el patron `owner/repo`; formulae y casks solo contengan `[a-z0-9._-]`; rechazar entradas que no cumplan el patron | Pendiente |
| SEG-007 | Seguridad | Watermark con email sin consentimiento | El email del comprador se incrusta en texto visible (`Licensed to: email`) y en caracteres invisibles Unicode de ancho cero en cada perfil exportado, sin informar al usuario. | Media | El usuario puede compartir perfiles sin saber que revelan su identidad de compra; violacion de expectativa de privacidad | `src/lib/license/watermark.ts:8-13, 20-34` | Informar al usuario en la UI que los perfiles exportados contienen marca de agua con el email; ofrecer exportacion sin PII o con hash del email | Pendiente |
| SEG-008 | Seguridad | NODE_ENV no inyectado por tsup | `brew-store.ts:112` usa `process.env.NODE_ENV !== 'production'` para guard de logs, pero `tsup.config.ts` no inyecta `NODE_ENV`. La guarda nunca activa el modo produccion y los `console.error` de debug se emiten en produccion. | Media | Exposicion de mensajes de error internos en produccion; posible fuga de informacion de diagnostico a usuarios finales | `src/stores/brew-store.ts:112`; `tsup.config.ts` sin bloque `define` | Agregar `"process.env.NODE_ENV": '"production"'` en el bloque `define` de `tsup.config.ts` | Pendiente |
| SEG-009 | Seguridad | npm publish sin SLSA provenance | `npm publish` en ambos workflows no incluye `--provenance`, por lo que los artefactos publicados en npm no tienen SLSA attestation verificable. | Media | Los usuarios no pueden verificar criptograficamente que el artefacto npm fue construido desde el repositorio declarado; riesgo de cadena de suministro | `.github/workflows/release.yml:26`; `.github/workflows/publish.yml:18` | Agregar `--provenance` y `permissions: id-token: write` en ambos workflows | Pendiente |
| ARQ-003 | Arquitectura | PackageInfoView duplica estado | `PackageInfoView` carga y almacena la formula con `useState` local via `api.getFormulaInfo()`, mientras `brew-store` ya tiene los datos de formulae. Hay dos fuentes para el mismo dato con ciclos de vida distintos. | Media | Posible inconsistencia de datos entre la vista de detalle y el store global; doble llamada a la CLI de brew | `src/views/package-info.tsx:32-33` | Agregar `selectedFormulaDetails` al `brew-store.ts` y accion `fetchFormulaDetails(name)`; la vista lee del store | Pendiente |
| ARQ-004 | Arquitectura | SmartCleanupView con logica de negocio | `SmartCleanupView` analiza texto del output de `brew uninstall` para detectar el tipo de error de dependencias dentro de la vista. Es parsing de texto CLI dentro del componente de presentacion. | Media | Logica de dominio acoplada a la vista; imposible testear de forma unitaria sin renderizar la vista completa | `src/views/smart-cleanup.tsx:33-34` | Mover la deteccion de errores de dependencia a `lib/cleanup/` como funcion `classifyUninstallError(output)`; la vista recibe un tipo de error estructurado | Pendiente |
| ARQ-005 | Arquitectura | Sin AbortController en OSV y Polar | `osv-api.ts` y `polar-api.ts` usan `fetch()` sin `AbortController`. Si el usuario navega fuera de `security-audit` durante un scan, el request HTTP continua en background sin posibilidad de cancelacion. (Relacionado con BK-001 pero este cubre especificamente la falta de cancelacion desde la UI.) | Media | Consumo innecesario de recursos de red; requests huerfanos que pueden causar actualizaciones de estado post-unmount | `src/lib/security/osv-api.ts`; `src/lib/license/polar-api.ts`; `src/views/security-audit.tsx` — sin cleanup en useEffect | Agregar `AbortController` y pasar la signal al `fetch`; retornar cleanup desde el `useEffect` de security-audit | Pendiente |
| CON-003 | Concurrencia | Brew-store sin TTL | Los datos de formulae, casks, outdated y services se cargan con `fetchAll()` al iniciar y permanecen en memoria sin expiracion. No hay timestamp de ultima carga ni indicacion visual de datos potencialmente obsoletos. Cada vista relanza su fetch al montar sin verificar frescura. | Media | Datos obsoletos mostrados al usuario sin indicacion; llamadas redundantes a brew CLI al navegar entre vistas | `src/stores/brew-store.ts` — ausencia de `lastFetchedAt` y threshold de frescura | Agregar `lastFetchedAt: Record<string, number>` al store; mostrar timestamp de ultima actualizacion en el Dashboard; no refetchear si los datos tienen menos de 30s | Pendiente |
| CON-004 | Concurrencia | AppState.refresh() secuencial en BrewBar | `checkOutdated()` y `checkServices()` en BrewBar se ejecutan secuencialmente (latencia total = suma de ambos). | Media | Latencia de refresco innecesariamente alta; cada check del scheduler tarda el doble de lo necesario | `menubar/BrewBar/Sources/Models/AppState.swift:25,32` | Usar `async let`: `async let outdated = checker.checkOutdated(); async let services = checker.checkServices()` | Pendiente |
| CON-005 | Concurrencia | Timer de badge a 2 segundos | `AppDelegate.swift:40` ejecuta un timer a 2 segundos continuo para actualizar el badge del status item. El conteo de paquetes desactualizados no cambia con frecuencia de 2s. | Media | Consumo energetico innecesario; 43.200 wake-ups del timer por dia | `menubar/BrewBar/Sources/App/AppDelegate.swift:40` — `Timer.scheduledTimer(withTimeInterval: 2, repeats: true)` | Aumentar el intervalo a 30-60 segundos | Pendiente |
| UI-007 | UI | AccountView congelada si deactivate lanza | `onConfirm` en AccountView llama `await deactivate()` sin try/catch. Si lanza excepcion, `setDeactivating(false)` nunca se ejecuta y `deactivating: true` bloquea el input de teclado indefinidamente via `useInput`. | Media | La UI de la vista de cuenta queda completamente inutilizable si falla la desactivacion; el usuario no puede salir sin reiniciar la app | `src/views/account.tsx:35-40` | Envolver en try/catch; llamar `setDeactivating(false)` en `finally`; mostrar el error al usuario | Pendiente |
| UI-008 | UI | AccountView muestra 'free' antes de validacion | El estado inicial del store es `status: 'free'` antes de que `initialize()` complete. El usuario puede ver momentaneamente que no es Pro aunque lo sea. | Media | Parpadeo de estado incorrecto en el arranque; poca confianza del usuario en el estado de su licencia | `src/stores/license-store.ts` — estado inicial `status: 'free'`; `src/views/account.tsx:49-51` | Inicializar `status` como `'validating'` hasta que `initialize()` complete la primera verificacion | Pendiente |
| UI-009 | UI | ServicesView errors de accion ignorados | El store almacena errores de `serviceAction` en `errors['service-action']`, pero `ServicesView` solo verifica `errors.services`. Si una accion (start/stop/restart) falla, el usuario no ve ningun mensaje de error. | Alta | Fallos de acciones de servicio completamente invisibles para el usuario; puede creer que la accion tuvo exito cuando fallo | `src/views/services.tsx:52-53` vs `src/stores/brew-store.ts:168` | Agregar `{errors['service-action'] && <ErrorMessage />}` en `ServicesView` | Pendiente |
| UI-010 | UI | padEnd hardcodeados para columnas | `padEnd(27)`, `padEnd(12)`, `padEnd(20)` hardcodeados para columnas de tablas. En terminales de menos de 80 columnas los valores se truncan o se desfasan. | Media | Layout roto en terminales estrechas (muy comun en macOS con terminal reducido) | `src/views/installed.tsx` — `pkg.name.padEnd(27)`; `src/views/services.tsx` — `svc.name.padEnd(20)` | Calcular anchos de columna a partir de `stdout.columns` con `useStdout()`; usar fracciones del total | Pendiente |
| UI-011 | UI | key={i} con identidad inestable | `key={i}` (indice de array) usado en `DoctorView` para warnings y en `ProgressLog` para lineas de streaming. En el buffer rolling de ProgressLog, el indice 0 representa distintas lineas en cada render. | Media | Diffing incorrecto en React; posibles problemas de renderizado y parpadeo en el ProgressLog durante streaming activo | `src/views/doctor.tsx` — `doctorWarnings.map((warning, i) => <Box key={i}`; `src/components/common/progress-log.tsx` — `visible.map((line, i) => <Text key={i}` | Usar `key={warning.slice(0,40)}` en DoctorView; usar `key={startIndex + i}` en ProgressLog donde `startIndex = lines.length - visible.length` | Pendiente |
| UI-012 | UI | app.tsx mezcla routing y efectos | `app.tsx` combina routing (switch de 12 casos), gate Pro, y efecto de inicializacion de licencia en el mismo componente. `useEffect(() => { initLicense(); }, [])` con dep array incompleto. | Media | Componente con demasiadas responsabilidades; dificil de mantener y testear | `src/app.tsx:10-20` | Extraer `<LicenseInitializer>` como componente separado; extraer `renderView` a un `ViewRouter` independiente | Pendiente |
| QA-002 | Testing | Ausencia de crash reporting | No existe ningun SDK de crash reporting en ninguno de los dos codebases. Los crashes en produccion de BrewBar son completamente invisibles para el equipo. 23+ bloques `catch { }` silenciosos en TypeScript descartan errores de diagnostico sin traza. | Alta | Imposible diagnosticar ni priorizar bugs en produccion; tasa de crash desconocida | Ausencia de Crashlytics, Sentry, Bugsnag en `package.json` y `Project.swift`; 23 bloques `catch` silenciosos identificados | Integrar Sentry (npm SDK para TUI, Swift SDK para BrewBar); agregar `process.on('uncaughtException', ...)` con logging antes de re-throw | Pendiente |
| QA-003 | Testing | Ausencia de analytics | Cero implementaciones de analytics en ambos codebases. El funnel de activacion, conversion Free-to-Pro, y uso de features Pro son completamente opacos. | Alta | Imposibilidad de tomar decisiones de producto basadas en datos; no se puede medir la conversion ni la retencion | Ausencia de cualquier `track()`, `logEvent()`, o SDK de analytics en `src/` y `menubar/` | Integrar analytics minimo: `feature_viewed`, `upgrade_prompt_shown`, `license_activated`, features Pro usadas; considerar PostHog self-hosted para privacidad | Pendiente |
| QA-004 | Testing | Logging estructurado ausente | No existe ninguna capa de logging estructurado en ningun codebase. Los 21 `console.log/error` son el unico mecanismo de traza. Errores criticos de red, de licencia y de operaciones brew se pierden silenciosamente. | Media | Imposible diagnosticar problemas en produccion; `LicenseChecker.decrypt()` falla silenciosamente en Swift sin ninguna traza | Ausencia de winston, pino, OSLog en todo el proyecto; `NSLog` solo en un punto en Swift | Crear `src/lib/logger.ts` con niveles debug/info/warn/error escribiendo a `~/.brew-tui/brew-tui.log`; adoptar `os.Logger` en BrewBar con subsistema `com.molinesdesigns.brewbar` | Pendiente |
| PERF-001 | Rendimiento | scryptSync en import de modulo | `scryptSync` con N=16384 iteraciones se ejecuta en tiempo de import de `license-manager.ts`, bloqueando el event loop sincrono antes del primer frame. Ademas `readFileSync` en `integrity.ts:23` ocurre en el mismo path critico de arranque. | Alta | Bloqueo del event loop durante el arranque; el TUI tarda varios cientos de ms adicionales en mostrar el primer frame | `src/lib/license/license-manager.ts:65` — `const _derivedKey = scryptSync(...)`; `src/lib/license/integrity.ts:23` — `_baselineHash = _captureBaseline()` | Mover a inicializacion lazy con memoizacion: `if (!_key) _key = scryptSync(...); return _key;`; usar `readFile` async en `integrity.ts` | Pendiente |
| PERF-002 | Rendimiento | fetchAll serializa brew update | `fetchAll()` en `brew-store.ts:136-138` espera `brew update` (~5-30s de red) antes de iniciar las cargas paralelas de datos. La UI queda en estado loading hasta que `brew update` termina. | Alta | El usuario espera hasta 30 segundos viendo solo un spinner antes de poder ver cualquier dato de la app | `src/stores/brew-store.ts:136-138` — `await api.brewUpdate()` bloqueante | Ejecutar `brew update` en background sin await: `api.brewUpdate().catch(() => {})`; iniciar `Promise.all` de datos inmediatamente despues | Pendiente |
| PERF-003 | Rendimiento | verifyPro() con I/O sincrona por operacion | `checkBundleIntegrity()` en `pro-guard.ts` ejecuta `readFileSync` en cada invocacion de `requirePro()`, que ocurre en cada operacion de cleanup, historial y perfiles Pro. El hash del bundle no cambia durante la ejecucion. | Media | I/O sincrona innecesaria en cada operacion Pro; degradacion de rendimiento acumulativa en sesiones largas | `src/lib/license/pro-guard.ts` — `checkBundleIntegrity()` llama `readFileSync` sin cache | Cachear el resultado de `checkBundleIntegrity()` con un flag de sesion o TTL de 60s | Pendiente |
| PERF-004 | Rendimiento | Busy-polling en streamBrew | `streamBrew()` usa `await new Promise((r) => setTimeout(r, 50))` en bucle activo durante todo el streaming de operaciones brew. Genera 20 wake cycles por segundo por operacion en curso. | Media | Consumo innecesario de CPU durante instalaciones y upgrades; degrada la responsividad de la UI en operaciones largas | `src/lib/brew-cli.ts:61-64` | Reemplazar el polling con eventos del readable stream: `process.stdout.on('data', ...)`; usar `AsyncIterator` nativo | Pendiente |
| PERF-005 | Rendimiento | NSHostingController recreado en cada apertura | `AppDelegate` crea un nuevo `NSHostingController(rootView: PopoverView(...))` en cada apertura del popover, destruyendo y reconstruyendo todo el arbol SwiftUI. | Media | Construccion innecesaria del arbol de vistas SwiftUI en cada clic; latencia perceptible al abrir el popover | `menubar/BrewBar/Sources/App/AppDelegate.swift:175` | Crear el `hostingController` como propiedad del `AppDelegate` en `applicationDidFinishLaunching` y reutilizarlo; actualizar solo el `AppState` observable | Pendiente |
| REL-002 | Release | publish.yml sin typecheck ni build | `publish.yml` ejecuta solo `npm ci` y `npm publish` sin `npm run typecheck` ni `npm run build`. Publica el contenido del directorio `build/` existente sin verificacion. | Alta | Posibilidad de publicar a npm artefactos no compilados o con errores de tipos no detectados | `.github/workflows/publish.yml:17-22` | Agregar `npm run typecheck && npm run build` antes de `npm publish` | Pendiente |
| REL-003 | Release | Version de BrewBar sin inyeccion en CI | `CFBundleShortVersionString: 1.0` esta hardcodeada en el Info.plist generado. El job `build-brewbar` no inyecta la version del tag de release. `Project.swift` no declara `MARKETING_VERSION`. | Media | La app distribuida siempre reporta version 1.0 independientemente del tag de release; imposible diagnosticar versiones en produccion | `menubar/Derived/InfoPlists/BrewBar-Info.plist:23`; `menubar/Project.swift` sin `MARKETING_VERSION` | Inyectar la version desde el tag git en el job CI via `-MARKETING_VERSION` al comando `xcodebuild` | Pendiente |
| REL-004 | Release | Ausencia de CHANGELOG | No existe `CHANGELOG.md` ni `RELEASE_NOTES.md`. El workflow genera notas automaticas desde commits sin estructura semantica. | Media | Usuarios y mantenedores no tienen visibilidad de cambios por version; dificulta el soporte y la comunicacion con usuarios | Ausencia de `CHANGELOG.md` en la raiz del repositorio | Crear `CHANGELOG.md` siguiendo Keep a Changelog; automatizar la actualizacion en el CI | Pendiente |
| REL-005 | Release | SHA256 del cask puede desincronizarse | `homebrew/Casks/brewbar.rb` y `homebrew/Formula/brew-tui.rb` tienen checksums SHA256 hardcodeados para la version 0.1.0. En futuros releases, el proceso manual puede desincronizarlos. | Media | El cask de Homebrew fallara en la instalacion si el SHA256 no coincide con el artefacto publicado | `homebrew/Casks/brewbar.rb:3`; `homebrew/Formula/brew-tui.rb:5` | Automatizar la actualizacion del SHA256 en el CI post-publicacion; o usar `using: :homebrew_tap` con verificacion automatica | Pendiente |
| SCR-001 | Pantallas | Colores hardcodeados para terminal oscura | Todas las 12 vistas TUI usan colores hex hardcodeados que asumen fondo de terminal oscura (`#F9FAFB`, `#9CA3AF`, `#06B6D4`, etc.). En terminales de fondo claro (comun en macOS con tema "Basic") el texto es practicamente invisible. | Media | La app es inutilizable en terminales de fondo claro, que son comunes en macOS; impacto en un subconjunto significativo de usuarios | Patron consistente en todos los archivos `src/views/*.tsx` | Documentar explicitamente el requisito de terminal oscura en el README; opcionalmente usar colores ANSI semanticos relativos al fondo o paleta con contraste relativo | Pendiente |
| SCR-002 | Pantallas | Cero deteccion de estado offline | Ninguna de las 12 vistas TUI verifica conectividad antes de iniciar operaciones de red. Los errores de red son indistinguibles de errores de brew. | Media | El usuario no sabe si la falla es de conectividad o de la herramienta; no puede tomar accion correctiva especifica | Ausencia de cualquier comprobacion de red en todas las vistas TUI | Implementar hook `useNetworkStatus()` que detecte conectividad y exponga un banner global; distinguir errores de red de errores de brew | Pendiente |
| UX-006 | UX | Onboarding ausente | No existe ningun flujo de bienvenida, tutorial ni guia inicial en ninguno de los dos codebases. El primer arranque lanza directamente el Dashboard sin orientacion. | Media | Los usuarios nuevos no saben los atajos de teclado ni las funcionalidades Pro disponibles; tasa de descubrimiento de features baja | Ausencia de cualquier archivo de onboarding o welcome en `src/` y `menubar/` | Implementar al menos una pantalla de bienvenida al primer arranque con instrucciones de atajos de teclado basicos e indicacion de features Pro | Pendiente |
| UX-007 | UX | Errores de serviceAction invisibles | El store almacena errores de `serviceAction` en `errors['service-action']` pero la vista solo renderiza `errors.services`. Acciones de servicio fallidas son completamente invisibles para el usuario. | Alta | Ver UI-009 — mismo hallazgo consolidado con contexto UX | `src/views/services.tsx:52-53` | Ver accion de UI-009 | Pendiente |
| EP-001 | Endpoints | Deactivate deja slot huerfano en Polar | La funcion `deactivate` borra la licencia local incondicionalmente aunque la llamada API falle. Si Polar no confirma la desactivacion, el slot de activacion queda activo en Polar pero la licencia ya fue borrada localmente. (Consolidado con SEG-005.) | Critica | Ver SEG-005 | Ver SEG-005 | Ver SEG-005 | Pendiente |
| EP-002 | Endpoints | Fallback OSV queryOneByOne silencia paquetes | En el fallback `queryOneByOne` de `osv-api.ts`, los errores de paquetes individuales se silencian con `catch { // Skip }`. El usuario ve resultados como si esos paquetes no tuvieran vulnerabilidades cuando en realidad no fueron consultados. | Alta | Falsos negativos en el Security Audit Pro; el usuario puede creer que su sistema es seguro cuando hay paquetes no consultados | `src/lib/security/osv-api.ts:112-113` | Registrar los paquetes omitidos; mostrar en la UI un contador de "N paquetes no consultados" para informar de la completitud del scan | Pendiente |
| EP-003 | Endpoints | execBrew sin timeout | `execBrew()` no tiene timeout. `brew doctor` puede tardar 10-30s; `brew install` puede colgarse si hay un lock de Homebrew activo. (Incluido en BK-001 como hallazgo transversal de timeouts.) | Alta | Ver BK-001 | `src/lib/brew-cli.ts:4-21` | Ver BK-001 — agregar timeout configurable en `execBrew` | Pendiente |
| GOV-005 | Gobierno | gitignore sin patrones de certificados | `.gitignore` no incluye patrones para `*.p12`, `*.cer`, `*.mobileprovision`, `ExportOptions.plist`, `AuthKey_*.p8`. Si se agregan certificados de firma en el futuro, no existira proteccion por defecto. | Baja | Riesgo de commit accidental de certificados de firma o claves de API de Apple | `.gitignore` — 16 lineas sin patrones para artefactos de firma Swift | Agregar al `.gitignore`: `*.p12`, `*.cer`, `*.mobileprovision`, `ExportOptions.plist`, `AuthKey_*.p8` | Pendiente |
| GOV-006 | Gobierno | Scheme con customWorkingDirectory hardcodeado | El scheme `Generate Project` en el xcworkspace tiene `customWorkingDirectory = "/Volumes/SSD/Projects/Brew/menubar"` apuntando a una ruta local del desarrollador original, distinta de la ruta real del proyecto. | Baja | Confusion para nuevos colaboradores; el scheme no funciona en ninguna otra maquina | `BrewBar.xcworkspace/xcshareddata/xcschemes/Generate Project.xcscheme:26` | Corregir al path relativo o documentar que debe regenerarse con `tuist generate` | Pendiente |
| ARQ-006 | Arquitectura | useBrewStream contiene logica de dominio | `use-brew-stream.ts` contiene `detectAction()` y `logToHistory()`, que son logica de dominio del modulo de historial, no logica de UI. | Media | Logica de negocio en un hook de UI; imposible testear de forma unitaria sin el ciclo de vida de React | `src/hooks/use-brew-stream.ts:7-36` | Mover `detectAction` y `logToHistory` a `src/lib/history/history-logger.ts`; el hook solo debe manejar el ciclo de vida del stream | Pendiente |
| ARQ-007 | Arquitectura | useBrewCommand hook sin usar | `src/hooks/use-brew-command.ts` es un hook generico que no se usa en ninguna vista ni componente. Codigo muerto. | Baja | Confusion para nuevos colaboradores; el hook puede malinterpretarse como la forma correcta de invocar comandos brew | `src/hooks/use-brew-command.ts` — 0 importaciones en todo el proyecto | Eliminar el archivo o documentarlo explicitamente como placeholder | Pendiente |
| ARQ-008 | Arquitectura | Patron loading/error duplicado en 8+ vistas | El patron `if (loading.X) return <Loading />; if (errors.X) return <ErrorMessage />;` se repite identicamente en 8 vistas sin extraccion. | Baja | Duplicacion de codigo; cambios en el patron de error requieren modificar 8 archivos | Patron en `dashboard.tsx`, `installed.tsx`, `outdated.tsx`, `services.tsx`, `doctor.tsx`, `smart-cleanup.tsx`, `history.tsx`, `security-audit.tsx` | Extraer un HOC o hook `useViewState(key)` que retorne el estado tipado | Pendiente |
| REL-006 | Release | NSLog sin guarda de build en BrewBar | `NSLog("[BrewBar] Failed to open Brew-TUI: %@", ...)` en `PopoverView.swift:181` sin guarda `#if DEBUG`. Aparecera en logs de produccion. | Baja | Fuga de mensajes de debugging en builds de produccion; no es una vulnerabilidad pero es un code smell | `menubar/BrewBar/Sources/Views/PopoverView.swift:181` | Reemplazar con `Logger` (OSLog) usando nivel `.debug` que no aparece en Release builds | Pendiente |
| BK-007 | Backend | historyEntry IDs con colision teorica | Los IDs de entradas de historial combinan `Date.now()` con `Math.random().toString(36)`. Bajo alta concurrencia `Date.now()` puede repetirse. | Baja | Riesgo teorico de colision de IDs en el historial; practicamente irrelevante dado el uso single-threaded | `src/lib/history/history-logger.ts:39` | Sustituir por `crypto.randomUUID()` (disponible en Node.js 14.17+) | Pendiente |
| UX-008 | UX | package-info accesible via Tab sin paquete | `package-info` esta en `VIEWS` y es alcanzable via Tab sin `selectedPackage` establecido. La pantalla muestra un mensaje de error sin hint de como navegar a un paquete. | Baja | El usuario puede llegar a un estado de error confuso sin guia de salida | `src/stores/navigation-store.ts:15` — `package-info` en `VIEWS`; `src/views/package-info.tsx` — mensaje sin indicar tecla de navegacion | Excluir `package-info` del ciclo de Tab (como se hace en el header pero no en el store) o agregar hint `2:Installed esc:back` en ese estado | Pendiente |
| UX-009 | UX | account sin atajo numerico | La vista `account` no tiene atajo numerico (solo accesible via Tab); es la unica vista Pro-relevante sin acceso rapido. | Baja | El usuario no puede acceder rapidamente a la gestion de su licencia desde cualquier vista | `src/components/layout/header.tsx:44` — `VIEW_KEYS[account] = ''` | Asignar atajo numerico o documentar explicitamente que es solo-Tab | Pendiente |

# 21. Registro central de hallazgos

> Auditor: report-consolidator | Fecha: 2026-05-01

## Resumen

* **Total hallazgos:** 98
* **Criticos:** 2
* **Altos:** 22
* **Medios:** 38
* **Bajos:** 36
* **Reportes analizados:** 14 de 14
* **Reportes faltantes:** Ninguno

---

## Notas metodologicas

Los IDs existentes en sub-reportes se han conservado verbatim (PERF-xxx, SCR-12-xxx). Los hallazgos sin ID previo reciben un ID nuevo segun la convencion de prefijos. Los hallazgos citados transversalmente en multiples reportes (p.ej. el bug OSV aparece en 07, 10 y 13) se registran **una sola vez** con referencia cruzada de evidencia. Los hallazgos con estado "Informativo" o "Conforme" en los sub-reportes no se incluyen salvo que aporten riesgo real.

---

## Registro completo

| ID | Dominio | Subzona | Hallazgo | Severidad | Impacto | Evidencia | Accion | Estado |
|----|---------|---------|----------|-----------|---------|-----------|--------|--------|
| SEG-001 | Seguridad | Supply chain / npm | Dos tokens npm con credenciales reales de publicacion en `.claude/settings.local.json` en texto plano. El archivo no esta excluido por el `.gitignore` del repo (si lo esta por el gitignore global del usuario), con blast radius de publicacion maliciosa de `brew-tui` afectando a todos los usuarios | Critica | Publicacion maliciosa del paquete npm `brew-tui` en npmjs.com; comprometeria a todos los usuarios del CLI | `.claude/settings.local.json:56,58` — dos tokens `npm_*` | Revocar ambos tokens en npmjs.com de inmediato. Agregar `.claude/` al `.gitignore` del repo. Usar tokens con scope `publish` restringido o GitHub Actions OIDC | Pendiente |
| BK-001 | Backend | OSV API — TS | `ecosystem: 'Homebrew'` enviado a OSV.dev en tres lugares; la API rechaza con HTTP 400. La excepcion se captura y trata como "0 CVEs encontrados". El Security Audit del TUI devuelve sistematicamente cero vulnerabilidades. La correccion a `'Bitnami'` existe en el lado Swift pero nunca se propago al TS | Critica | El feature Pro Security Audit esta completamente roto: no detecta ninguna vulnerabilidad en ninguna instalacion, devolviendo falsa sensacion de seguridad a usuarios Pro | `src/lib/security/osv-api.ts:125,143,181`. Comparar con `menubar/BrewBar/Sources/Services/SecurityMonitor.swift:119` (correcto) | Cambiar las tres ocurrencias de `'Homebrew'` a `'Bitnami'` en `osv-api.ts:125,143,181` | Pendiente |
| SEG-002 | Seguridad | Cifrado de licencias — bundle npm | `ENCRYPTION_SECRET = 'brew-tui-license-aes256gcm-v1'` y `SCRYPT_SALT = 'brew-tui-salt-v1'` hardcodeados en bundle npm publicado. Cualquier usuario puede derivar la clave AES-256-GCM con `scryptSync` y fabricar un `license.json` valido localmente | Alta | Elusibilidad del sistema de licencias: un atacante puede generar licencias Pro/Team sin pagar. Mitigado parcialmente por revalidacion server-side (24h) y machine binding | `src/lib/license/license-manager.ts:78-79`; `src/lib/sync/crypto.ts:6-7` | Incorporar `machineId` como salt adicional en `scryptSync`; considerar migrar la clave derivada a macOS Keychain | Pendiente |
| SEG-003 | Seguridad | Cifrado de licencias — binario Swift | Clave AES-256-GCM pre-derivada como literal hexadecimal de 64 caracteres en el binario Swift de BrewBar. Recuperable con `strings BrewBar.app/Contents/MacOS/BrewBar` sin herramienta especializada | Alta | La clave de 256 bits expuesta permite descifrar cualquier `license.json` sin necesidad del bundle npm. Equivale a SEG-002 pero por ruta diferente | `menubar/BrewBar/Sources/Services/LicenseChecker.swift:50-53`: `let hex = "5c3b2ae2a3066bca28773f36db347d8c8a0a396d4b9fab628331446acd6d4126"` | Eliminar el literal hex precomputado; rederivarlo en runtime con los mismos literales de constante (misma exposicion teorica pero sin el hex directo). Solucion duradera: Keychain | Pendiente |
| GOV-001 | Gobierno | CI/CD — pipeline Swift | No existe ningun workflow CI que compile, archive, firme ni notarice BrewBar. El unico CI es `ubuntu-latest` con `npm run validate`. La release de BrewBar es 100% manual y los tests Swift nunca se validan automaticamente | Alta | Regresiones Swift pueden llegar a produccion sin deteccion; builds de release sin verificacion automatica | `.github/workflows/ci.yml` — runner `ubuntu-latest`, sin pasos Swift | Agregar job `test-swift` con runner `macos-latest` que ejecute `tuist generate && xcodebuild test -scheme BrewBar` | Pendiente |
| GOV-002 | Gobierno | Release — Homebrew Cask | `brewbar.rb` version `0.1.0` con URL de descarga apuntando a usuario GitHub `MoLinesGitHub` (incorrecto). La URL produce HTTP 404 incluso despues de un bump de version. Canal de distribucion completamente roto | Alta | Usuarios que instalan BrewBar via Homebrew Cask obtienen HTTP 404 — instalacion fallida. Canal de distribucion inoperativo | `homebrew/Casks/brewbar.rb:2,5` — `version "0.1.0"`, URL con `MoLinesGitHub` | Corregir URL a `MoLinesDesigns`, actualizar version a `0.6.1` y SHA256. Verificar con `brew audit --cask brewbar` | Pendiente |
| GOV-003 | Gobierno | Release — Homebrew Formula | `brew-tui.rb` version `0.5.3` — dos versiones menores atrasada respecto a `0.6.1`. Usuarios que instalan via `brew install` reciben version sin los fixes de `0.6.0` y `0.6.1` | Alta | Usuarios de Homebrew Formula reciben version desactualizada sin correcciones de seguridad y bugs | `homebrew/Formula/brew-tui.rb:3,4` — `url` apunta a `brew-tui-0.5.3.tgz` | Actualizar URL y SHA256 a `0.6.1` | Pendiente |
| GOV-004 | Gobierno | Release — jsr.json | `jsr.json` version `0.5.2` mientras `package.json` es `0.6.1`. `publish-all.sh` publicaria `0.5.2` en JSR en el proximo ciclo de release | Alta | Publicacion de version obsoleta en JSR; usuarios de Deno/JSR reciben version sin fixes | `jsr.json:3` — `"version": "0.5.2"` | Sincronizar `jsr.json` con `package.json` como primer paso de `publish-all.sh` | Pendiente |
| GOV-005 | Gobierno | Release — notarizacion BrewBar | BrewBar no esta notarizado. No existe ningun paso `xcrun notarytool` en scripts ni CI. `exportOptions.plist` tiene `method: "none"`. Gatekeeper bloquea o advierte a los usuarios que descargan el zip directamente | Alta | Usuarios que instalan BrewBar via descarga directa o Homebrew Cask son bloqueados por Gatekeeper macOS Ventura/Sonoma, impidiendo la ejecucion | `menubar/exportOptions.plist` — `method: none`; ausencia de `notarytool` en `scripts/` y `.github/workflows/` | Agregar `xcrun notarytool submit --wait` y `xcrun stapler staple` al script de release; actualizar `exportOptions.plist` a `method: developer-id` | Pendiente |
| GOV-006 | Gobierno | Privacy manifest | `PrivacyInfo.xcprivacy` declara `NSPrivacyAccessedAPICategoryFileTimestamp` (razon `C617.1`) sin ninguna llamada a APIs de timestamps de fichero en el codigo Swift. Apple Review puede rechazar entradas no justificadas | Alta | Riesgo de rechazo en App Store Review o distribucion notarizada; ademas, declara acceso que no existe | `PrivacyInfo.xcprivacy:14-21`; busqueda exhaustiva en `menubar/BrewBar/Sources/**` sin `attributesOfItem`, `creationDate`, `modificationDate`, `contentModificationDate` ni `resourceValues` | Eliminar el bloque `NSPrivacyAccessedAPICategoryFileTimestamp` del manifest; o identificar y documentar el uso real si existe | Pendiente |
| ARQ-001 | Arquitectura | Schemas de licencia — contrato TS/Swift | `src/lib/license/types.ts` define `LicenseData`/`LicenseFile` independientemente de los structs Swift equivalentes en `LicenseChecker.swift`. Leen el mismo archivo `~/.brew-tui/license.json` pero no hay test de contrato que verifique compatibilidad | Alta | Un cambio de campo en un lado puede romper silenciosamente la lectura en el otro, causando degradacion de licencia erronea o crashes | `src/lib/license/types.ts` vs `menubar/BrewBar/Sources/Services/LicenseChecker.swift` | Anadir test de contrato en `BrewBarTests` que lea un fixture de `license.json` generado por TS y verifique que Swift lo parsea correctamente | Pendiente |
| ARQ-002 | Arquitectura / Concurrencia | Politica de degradacion de licencia — divergencia | TS aplica gracia de 7 dias + degradacion progresiva hasta 30 dias. Swift (`LicenseChecker.swift`) trata la licencia como Pro plena hasta el dia 30 y luego la expira directamente. Mismo `license.json`, comportamiento inconsistente segun que app usa el usuario | Alta | Usuario offline entre 7 y 30 dias recibe estado "limitado" en el TUI pero "Pro pleno" en BrewBar — incoherencia que puede causar soporte y confusion | `src/lib/license/license-manager.ts:243-254` (TS) vs `menubar/BrewBar/Sources/Services/LicenseChecker.swift:expiredThreshold=30` | Extraer especificacion de degradacion a documento compartido; alinear umbrales en ambos lados | Pendiente |
| BK-002 | Backend | Resiliencia — deadlock BrewProcess | `readDataToEndOfFile()` sincrono en `terminationHandler` de `BrewProcess.swift`. Si el output supera ~64 KB (buffer de Pipe de macOS), `brew` bloquea en escritura, `terminationHandler` nunca se ejecuta y la continuacion cuelga. El timeout externo de 60s lo mata eventualmente | Alta | BrewBar puede quedar bloqueado hasta 60s al listar paquetes con muchas dependencias; la operacion falla silenciosamente. Potencial deadlock real | `menubar/BrewBar/Sources/Services/BrewProcess.swift:99` | Migrar a lectura incremental con `availableData` en loop o `AsyncStream` consumiendo el `FileHandle` en un `Task` separado | Pendiente |
| BK-003 | Backend / Persistencia | Snapshots sin poda | Cada post-upgrade, post-rollback y post-reconcile crea un snapshot nuevo en `~/.brew-tui/snapshots/` sin ninguna poda automatica. Con uso intensivo puede acumular cientos de archivos | Alta | Crecimiento ilimitado del directorio de snapshots; degradacion de rendimiento en listar snapshots y uso excesivo de disco | `src/lib/state-snapshot/snapshot.ts:95-107`; `src/lib/rollback/rollback-engine.ts:204-209` | Implementar `pruneSnapshots(maxCount = 20)` llamada tras cada `saveSnapshot` | Pendiente |
| UI-001 | UI | Vistas destructivas sin confirmacion — Brewfile | `BrewfileView`: reconciliacion inicia directamente con `c` sin `ConfirmDialog`. Puede instalar y desinstalar paquetes masivamente | Alta | Accion destructiva irreversible sin confirmacion; usuario puede aplicar cambios masivos accidentalmente | `src/views/brewfile.tsx:155-163` | Agregar `ConfirmDialog` con resumen del numero de paquetes afectados antes de iniciar reconciliacion | Pendiente |
| UI-002 | UI | Vistas destructivas sin confirmacion — Sync | `SyncView`: `syncNow` inicia directamente con `s` sin paso de confirmacion cuando puede sobrescribir el estado local | Alta | Sync puede sobrescribir estado local de paquetes sin confirmacion; accion irreversible sin undo | `src/views/sync.tsx:232` | Agregar `ConfirmDialog` antes de invocar `syncNow` | Pendiente |
| UI-003 | UI | Vistas destructivas sin confirmacion — Compliance | `ComplianceView`: remediacion inicia con `c` sin `ConfirmDialog`; puede instalar y desinstalar paquetes | Alta | Remediacion destructiva sin confirmacion; especialmente grave en contexto Team donde la policy puede ser restrictiva | `src/views/compliance.tsx:198-204` | Agregar `ConfirmDialog` con numero de violaciones accionables antes de iniciar | Pendiente |
| QA-001 | Testing | Vistas TUI sin tests de renderizado | Ninguna de las 16 vistas del TUI tiene tests de renderizado via `ink-testing-library`, que esta instalada pero sin uso. `ViewRouter`, `HomeView`, `UpdatesView`, `SecurityAuditView`, `AccountView` sin cobertura de render | Alta | Regresiones de UI invisibles; cambios en vistas criticas pueden romper estados de carga, error o navegacion sin deteccion | `src/views/*.tsx` — sin archivos `.test.tsx`; `ink-testing-library@^4.0.0` en `package.json` sin uso | Crear tests de renderizado para al menos `DashboardView`, `OutdatedView`, `AccountView` y `ViewRouter` | Pendiente |
| QA-002 | Testing | ViewRouter y componentes comunes sin tests | `ViewRouter` (switch exhaustivo en `app.tsx`) y componentes comunes criticos (`ConfirmDialog`, `UpgradePrompt`, `ProgressLog`) sin tests de renderizado | Alta | Cambios en el gate Pro/Team o en el routing pueden llegar a produccion sin verificacion | `src/app.tsx:35-67`; `src/components/common/*.tsx` — solo tests de logica en algunos, sin tests de render | Agregar `app.test.tsx` con casos free/pro/team; tests de render para `ConfirmDialog` y `UpgradePrompt` | Pendiente |
| QA-003 | Testing | Tests Swift fuera de CI | Tests de BrewBar (`BrewBarTests`, `ServiceTests`) nunca se ejecutan en CI. El unico runner es `ubuntu-latest` — Swift no compila en Ubuntu | Alta | Cambios en BrewBar pueden introducir regresiones Swift sin deteccion automatica | `.github/workflows/ci.yml` — runner `ubuntu-latest`, sin paso `xcodebuild test` | Agregar job `test-swift` con `macos-latest`, `tuist generate`, `xcodebuild test -scheme BrewBar` | Pendiente |
| UX-001 | UX | Notificaciones CVE/Sync con identificadores fijos | `SchedulerService.swift` usa `"brewbar-cve"` y `"brewbar-sync"` como identificadores de notificacion fijos. macOS reemplaza silenciosamente notificaciones con el mismo identificador — notificaciones CVE subsiguientes nunca aparecen como banner | Alta | Usuarios Pro no reciben alertas de vulnerabilidades CVE nuevas despues de la primera notificacion. Feature de notificacion de seguridad inoperativa en la practica | `menubar/BrewBar/Sources/Services/SchedulerService.swift:184,253` | Usar identificadores unicos con timestamp igual que las notificaciones de `outdated` | Pendiente |
| UX-002 | UX | Onboarding ausente | No existe ninguna pantalla de bienvenida ni guia inicial. Un usuario nuevo llega directamente al DashboardView con metricas vacias sin contexto | Alta | Alto abandono en primera sesion; usuarios nuevos no entienden el producto ni la navegacion | `src/views/dashboard.tsx` — primer render sin guia de uso | Agregar flujo de bienvenida condicional a `hasLaunchedKey` con instrucciones basicas de navegacion | Pendiente |
| BK-004 | Backend | `isExpired()` falla abierto en fecha invalida | `isExpired()` retorna `false` cuando la fecha de expiracion es `undefined` o ISO invalido (NaN < Date.now() es false). Esto significa que una licencia con fecha corrupta nunca expira | Alta | Licencias corruptas o manipuladas con fecha invalida se tratan como vigentes indefinidamente | `src/lib/license/license-manager.ts:214-217` — `new Date(undefined).getTime()` → NaN; `NaN < Date.now()` → false | Unificar: `isExpired` debe retornar `true` cuando la fecha es invalida o no parseable | Pendiente |
| DS-001 | Design system | Tokens de espaciado no adoptados | `SPACING` de `src/utils/spacing.ts` esta definido pero nunca importado en vistas ni componentes. 165+ valores numericos crudos en produccion | Alta | Inconsistencia visual; refactors de layout requieren busqueda manual de magic numbers en lugar de actualizar un token | `src/utils/spacing.ts` — `grep -r 'SPACING' src/views src/components` = 0 resultados | Migrar padding/margin/gap progresivamente a `SPACING.xs/sm/md/lg`; agregar regla ESLint `no-magic-numbers` para forzar la adopcion | Pendiente |
| ACC-001 | Accesibilidad | `NO_COLOR` no implementado | La TUI ignora la variable de entorno estandar `NO_COLOR` (freedesktop.org). En terminales que declaran esta variable, la app renderiza colores ANSI aunque el usuario los haya desactivado | Alta | Usuarios con terminales sin soporte de color o con lectores de pantalla reciben salida coloreada no deseada; puede romper scripts que procesan la salida | `src/utils/colors.ts:1-40` — sin rama `process.env.NO_COLOR` | En `src/utils/colors.ts`, retornar tokens vacios `''` si `process.env.NO_COLOR` esta definido | Pendiente |
| BK-005 | Backend | Cuatro implementaciones de `getMachineId` con fallbacks divergentes | `polar-api.ts`, `license-manager.ts`, `sync-engine.ts` y `promo.ts` implementan `getMachineId` de forma independiente con fallbacks distintos. `sync-engine.ts` cae a `hostname()`, lo que puede causar colision entre maquinas | Alta | En primera ejecucion, las cuatro rutas pueden llamarse en paralelo y crear el archivo con race condition. El fallback a `hostname()` en sync-engine puede causar que dos maquinas con el mismo hostname se sobreescriban mutuamente | `src/lib/sync/sync-engine.ts:53` (fallback a `hostname()`); `src/lib/license/polar-api.ts:38-48`; `src/lib/license/license-manager.ts:137-147`; `src/lib/license/promo.ts:11-20` | Extraer una funcion unica `getMachineId()` a `data-dir.ts` con mutex; eliminar todos los duplicados. Eliminar el fallback a `hostname()` en sync | Pendiente |
| ARQ-003 | Arquitectura | `SyncMonitor` sin protocolo de DI | `SyncMonitor` es un actor con `static let shared` sin protocolo `SyncMonitoring` equivalente. `SchedulerService` y `AppDelegate` acceden a `.shared` directamente, impidiendo tests del comportamiento de sync | Media | Sin seam de inyeccion, el comportamiento de sync no puede probarse en tests unitarios de `SchedulerService` | `menubar/BrewBar/Sources/Services/SyncMonitor.swift`; `SchedulerService.swift:165-166` — acceso directo a `.shared` | Crear protocolo `SyncMonitoring: Sendable` e inyectarlo en `SchedulerService.init` | Pendiente |
| ARQ-004 | Arquitectura | Estados de carga ad-hoc en `brew-store` | `loading: Record<string, boolean>` y `errors: Record<string, string \| null>` permiten estados logicamente inconsistentes (loading=true y error="" simultaneamente) | Media | Estados de interfaz inconsistentes posibles; logica de renderizado en vistas debe coordinar manualmente lo que un tipo discriminado haria automaticamente | `src/stores/brew-store.ts:22-24` | Introducir `type AsyncState<T>` discriminado con status `idle/loading/success/error` | Pendiente |
| ARQ-005 | Arquitectura | Cache CVE no compartida entre TUI y BrewBar | `CVE_CACHE_PATH` declarado en `data-dir.ts` pero ningun modulo TS lo importa. La TUI mantiene cache en memoria de 30 min. BrewBar escribe en disco con TTL de 1h. Los dos sistemas son independientes | Media | Un usuario puede ver resultados diferentes en BrewBar y en el TUI. La cache en disco de BrewBar es inaccesible para la TUI | `src/lib/data-dir.ts` — exporta `CVE_CACHE_PATH`; grep en `src/` retorna solo la declaracion; `SecurityMonitor.swift:269-280` escribe el archivo | Decidir el contrato de cache: TUI lee el archivo de BrewBar o TUI escribe su propia cache con TTL documentado | Pendiente |
| BK-006 | Backend | `decryptLicenseData` sin type guard post-descifrado | `JSON.parse(plaintext) as LicenseData` sin validacion de forma despues del descifrado AES. Un archivo de licencia migrado o corrompido puede causar acceso a propiedades indefinidas | Media | Acceso a propiedades de un objeto con forma incorrecta → crash silencioso o comportamiento indefinido. Baja probabilidad pero impacto alto si ocurre | `src/lib/license/license-manager.ts:121` | Agregar type guard `isLicenseData(data: unknown): data is LicenseData` con verificacion de campos requeridos | Pendiente |
| BK-007 | Backend | Lock de historial sin TTL — posible bloqueo permanente | Si el proceso muere entre `open('wx', lockPath)` y `unlink(lockPath)`, el lockfile queda huerfano y bloquea todas las escrituras futuras al historial indefinidamente | Media | El historial de operaciones queda inaccesible permanentemente hasta que el usuario borre manualmente el lockfile | `src/lib/history/history-logger.ts:13-24` | Comprobar `mtime` del lockfile: si tiene mas de 30s eliminarlo como lock huerfano antes de reintentar | Pendiente |
| BK-008 | Backend | `decryptPayload` sin type guard en sync | `JSON.parse(...) as SyncPayload` sin validacion de forma en el crypto de sync. Un envelope iCloud corrupto puede producir un objeto con forma incorrecta | Media | Corrupcion silenciosa del estado de sync; datos de paquetes incorrectos aplicados en la maquina local | `src/lib/sync/crypto.ts:53` | Agregar validacion de forma de `SyncPayload` tras parseo | Pendiente |
| BK-009 | Backend | Fallback de `machineId` a `hostname()` en sync | Si `~/.brew-tui/machine-id` no existe al sincronizar, `sync-engine.ts` usa `hostname()` como clave del mapa de maquinas. Dos maquinas con el mismo hostname colisionan y se sobrescriben mutuamente | Media | En redes de empresa o usuarios con mismo nombre de equipo, el sync puede sobrescribir datos de paquetes de otra maquina sin deteccion | `src/lib/sync/sync-engine.ts:53` | Crear `machine-id` en `ensureDataDirs()` como parte del setup inicial; eliminar el fallback a `hostname()` | Pendiente |
| BK-010 | Backend | Watermark con consentimiento opt-in no garantizado | `getWatermark(license, consent = true)` tiene `true` como valor por defecto; `embedInvisibleWatermark(text, email)` no recibe parametro de consentimiento. Cualquier llamador que omita el argumento incrusta el email del usuario silenciosamente | Media | Incrustacion de datos personales (email) en exports sin consentimiento explicito del usuario; riesgo de privacidad | `src/lib/license/watermark.ts` — `getWatermark(license, consent = true)`; `embedInvisibleWatermark(text, email)` sin param de consentimiento | Cambiar `consent = true` a `consent = false`; agregar `consent: boolean` como parametro requerido en `embedInvisibleWatermark` | Pendiente |
| BK-011 | Backend | Token crash reporter en UserDefaults — BrewBar | `CrashReporter.swift` almacena el token Bearer en `UserDefaults.standard`, que persiste en un plist en texto claro accesible por cualquier proceso del usuario | Media | Token de crash reporter legible por cualquier proceso del mismo usuario sin elevacion de privilegios | `menubar/BrewBar/Sources/Services/CrashReporter.swift:45-46` | Mover el token a Keychain (`kSecClassGenericPassword`) con `kSecAttrService="com.molinesdesigns.brewbar"` | Pendiente |
| BK-012 | Backend | iCloud: no se verifica estado de descarga del archivo | `icloud-backend.ts` puede leer datos parciales o ENOENT si el archivo esta en estado `placeholder` (no descargado) en iCloud. El error ENOENT se trata como "no existe todavia", enmascarando un placeholder no descargado | Media | Sync puede fallar silenciosamente o leer datos obsoletos cuando iCloud aun no ha descargado el archivo | `src/lib/sync/backends/icloud-backend.ts:35-55` | Verificar que el archivo tiene tamanyo > 0 antes de leer; o comprobar el estado de descarga iCloud | Pendiente |
| BK-013 | Backend | `SyncMonitor.swift` lee `sync.json` de forma sincrona | `Data(contentsOf: syncPath)` sincrono dentro de un `actor` puede bloquear si iCloud esta descargando el archivo | Media | Bloqueo del actor `SyncMonitor` durante operaciones de iCloud; la UI de BrewBar puede congelarse | `menubar/BrewBar/Sources/Services/SyncMonitor.swift:25` | Migrar a lectura asincrona con `URLSession` o `async let` con `url.resourceValues(forKeys:)` | Pendiente |
| BK-014 | Backend | Incompatibilidad de formato de severidad CVE entre TS y Swift | TS usa uppercase (`'CRITICAL'`); Swift usa lowercase raw values (`case critical`). Si el TUI llegara a leer el `cve-cache.json` escrito por Swift, el decodificado fallaria silenciosamente | Media | Inconsistencia futura si se unifica el cache; los datos de severidad no son intercambiables hoy | `src/lib/security/types.ts:1` — uppercase; `menubar/BrewBar/Sources/Models/CVEAlert.swift:12-18` — lowercase | Unificar convencion de casing; definir contrato de esquema JSON explicito | Pendiente |
| BK-015 | Backend | Promo API sin validacion de host | `promo.ts` usa `fetchWithTimeout` sin la funcion `validateApiUrl` que si existe en `polar-api.ts:51-59`. Un cambio en la constante URL podria apuntar a HTTP sin guardia | Media | Si la URL se modifica en un fork o por accidente, el token de autenticacion podria enviarse por HTTP | `src/lib/license/promo.ts:94,142` — sin llamada a `validateApiUrl` | Aplicar la misma guardia de URL a la Promo API | Pendiente |
| BK-016 | Backend | `brewUpdate()` sin timeout | `brewUpdate()` usa `spawn('brew', ['update'])` sin timer ni `AbortSignal`. Contrasta con `execBrew` que tiene timeout de 30s | Media | `brew update` puede colgar indefinidamente, bloqueando el arranque del store | `src/lib/brew-api.ts:18-26` | Refactorizar para reutilizar `execBrew` o anadir timeout explicito | Pendiente |
| BK-017 | Backend | `PrivacyInfo.xcprivacy` potencialmente incompleto | El manifest puede faltar `NSPrivacyAccessedAPICategoryDiskSpace` u otras categorias segun el uso real de `FileManager`. Apple Review puede rechazar entradas faltantes | Media | Rechazo en App Store Review o en la pipeline de notarizacion | `PrivacyInfo.xcprivacy:5-28` | Ejecutar `xcodebuild -generatePrivacyReport` y anadir los tipos faltantes | Pendiente |
| QA-004 | Testing | Sin medicion de cobertura de codigo | `vitest.config.ts` sin bloque `coverage`; `@vitest/coverage-v8` no esta instalado. No se produce ninguna metrica de lineas o branches cubiertas | Media | Sin cobertura medida es imposible detectar regresiones en cobertura o identificar zonas no probadas sistematicamente | `vitest.config.ts` — sin campo `coverage`; `package.json` — sin `@vitest/coverage-v8` | Agregar `@vitest/coverage-v8` como devDependency; configurar `coverage: { provider: 'v8', reporter: ['text', 'lcov'] }` en `vitest.config.ts` | Pendiente |
| QA-005 | Testing | `SecurityMonitor.swift` — `URLSession.shared` no inyectable | `URLSession.shared` hardcoded en `SecurityMonitor.swift`; sin protocolo de inyeccion. El path de red a OSV API no puede probarse en tests unitarios | Media | El flujo critico de consulta CVE no tiene tests de integracion ni con mock de red | `menubar/BrewBar/Sources/Services/SecurityMonitor.swift:134` | Refactorizar para aceptar `URLSession` como parametro init; anadir tests con mock | Pendiente |
| QA-006 | Testing | `OutdatedPackage` — cask sin campo `pinned` sin test de regresion | El test `jsonDecoding()` inyecta `pinned=true`, no prueba JSON de cask que omita la clave `pinned`. Fix de v0.6.1 sin test de regresion | Alta | Sin test de regresion, una regresion en el parsing de `pinned` puede no detectarse | `menubar/BrewBarTests/Sources/BrewBarTests.swift` — test con `pinned=true` solamente | Agregar test de regresion: JSON sin clave `pinned` debe decodificar con `pinned=false, pinnedVersion=nil` | Pendiente |
| QA-007 | Testing | BrewBar sin crash reporting SDK | BrewBar depende exclusivamente de los crash logs de macOS (opt-in del usuario). No hay SDK activo de crash reporting | Media | Los crashes de produccion son invisibles para el equipo a menos que el usuario los reporte manualmente | `menubar/Project.swift` — sin dependencia de Crashlytics, Sentry ni Bugsnag | Integrar Sentry Swift SDK (SPM) o Firebase Crashlytics con dSYM upload automatico | Pendiente |
| QA-008 | Testing | Ausencia total de analytics | Sin ninguna instrumentacion de eventos en TUI ni BrewBar. Los funnels de activacion Pro, uso de features y conversion son completamente invisibles | Alta | Sin datos de producto es imposible medir conversion, retencion ni impacto de cambios de UX | Busqueda de `track(`, `logEvent(`, `Analytics.` en `src/` y `menubar/`: 0 resultados | Definir taxonomia minima: `activation_started`, `activation_completed`, `feature_viewed`, `upgrade_prompt_shown`; integrar SDK con consentimiento | Pendiente |
| UX-003 | UX | Bug i18n — strings hardcodeados en sync.tsx y brewfile.tsx | `sync.tsx:127` tiene `"navegar"` y `"aplicar"` en espanol hardcodeados; `brewfile.tsx:70,261` tiene strings en ingles. Ambos rompen la localizacion en el locale contrario | Media | Usuarios en locale `en` ven texto espanol; usuarios en locale `es` ven texto en ingles. Rompe la confianza en el producto | `src/views/sync.tsx:127`; `src/views/brewfile.tsx:70,261` | Extraer a claves i18n en `en.ts`/`es.ts` | Pendiente |
| UX-004 | UX | Rate-limit de activacion solo en memoria | `activationAttempts` Map en memoria; el contador se resetea al reiniciar el proceso. La proteccion anti-abuso es ilusoria si el usuario reinicia el CLI | Media | Un atacante puede hacer fuerza bruta de claves de licencia reiniciando el proceso entre intentos; la barrera real esta solo en Polar server-side | `src/lib/license/license-manager.ts:34` — `const tracker: ActivationTracker` variable de modulo | Persistir intentos en disco o documentar explicitamente que el rate limit es un primer filtro | Pendiente |
| UX-005 | UX | Modelo de degradacion de licencia desincronizado (UX) | La degradacion aparece en el TUI en 7 dias pero BrewBar no la muestra hasta los 30 dias. El usuario ve mensajes contradictorios segun que app consulta (referencia cruzada con ARQ-002) | Media | Confusion del usuario; soporte innecesario. Misma fuente de datos, experiencia diferente | `src/lib/license/license-manager.ts:243-254` vs `LicenseChecker.swift:expiredThreshold=30` | Alinear umbrales (ver ARQ-002) | Pendiente |
| UX-006 | UX | Sin accion de revalidacion dentro del TUI | `AccountView` solo muestra la instruccion CLI `brew-tui revalidate` cuando la licencia esta degradada. El usuario debe salir del TUI para revalidar | Media | Fricccion innecesaria; usuarios degradados pueden no saber que hay un flujo CLI separado | `src/views/account.tsx` — solo muestra mensaje, sin accion inline | Agregar accion `revalidate` con keybind en `AccountView` para usuarios degradados | Pendiente |
| UX-007 | UX | `UpgradePrompt` sin claves para `rollback` y `brewfile` | `FEATURE_KEYS` en `upgrade-prompt.tsx` no incluye `rollback` ni `brewfile`. Si se alcanza esas vistas como usuario free (antes del gate en `app.tsx`), retorna `null` silenciosamente | Media | Vista en blanco en lugar de un prompt de upgrade descriptivo; usuario no sabe por que la vista esta vacia | `src/components/common/upgrade-prompt.tsx:9-16` | Agregar entradas `rollback` y `brewfile` a `FEATURE_KEYS` | Pendiente |
| UI-004 | UI | `SearchView` fuera del ciclo Tab/Shift-Tab | `search` no esta en el array `VIEWS` de `navigation-store.ts`. Solo accesible via `S` mayuscula, no via Tab cycling | Media | Usuarios que navegan por Tab no descubren la vista de busqueda; atajos inconsistentes | `src/stores/navigation-store.ts:14-17` — `search` ausente; `src/hooks/use-keyboard.ts:53` | Agregar `'search'` al array `VIEWS` o documentar explicitamente en footer | Pendiente |
| UI-005 | UI | `DashboardView` sin retry en error de carga | `ErrorMessage` se muestra cuando `errors.installed` es permanente pero sin boton ni hint de teclado para reintentar | Media | Usuario no puede recuperarse de un error de carga inicial sin salir y volver a entrar al dashboard | `src/views/dashboard.tsx:119-120` | Agregar hint `r:${t('hint_refresh')}` y conectar a `fetchAll()` cuando hay error | Pendiente |
| UI-006 | UI | `ProgressLog` key inestable provoca re-renders completos | Key calculada como `lines.length - visible.length + i` cambia en cada append de linea, haciendo que React trate la lista como completamente nueva en cada render | Media | Re-renders costosos durante operaciones de streaming largas; potencial jank visible en terminales lentos | `src/components/common/progress-log.tsx:26` | Cambiar la clave a un identificador estable basado en el indice absoluto de la linea | Pendiente |
| UI-007 | UI | `ServicesView` — mensaje especifico de permisos root ausente | Cuando el error de accion proviene de permisos insuficientes (`EACCES`/`sudo`), el mensaje es generico sin indicar que se requieren permisos elevados | Media | Usuario no sabe por que la accion falla; puede intentarla repetidamente sin entender el motivo | `src/views/services.tsx:52-65` | Detectar `EACCES`/`sudo` en el error y mostrar mensaje especifico | Pendiente |
| UI-008 | UI | `BrewBar PopoverView` — `refreshTask` no cancelada antes de crear nueva | La task de refresco no se cancela cuando el usuario vuelve a pulsar el boton, pudiendo haber dos tasks concurrentes actualizando `appState` | Media | Race condition potencial: dos refreshes concurrentes pueden actualizar `AppState` en orden incorrecto | `menubar/BrewBar/Sources/Views/PopoverView.swift:8,41` | Cancelar `refreshTask` antes de crear una nueva: `refreshTask?.cancel(); refreshTask = Task { ... }` | Pendiente |
| UI-009 | UI | `ServicesView` — logica de negocio en handler de `useInput` | `doAction` closure accede a `useBrewStore.getState()` directamente dentro del handler de `useInput` | Media | Acoplamiento de presentacion con acceso al store; dificulta tests y viola la separacion de capas | `src/views/services.tsx:53-65` | Mover la coordinacion de estado al store; la vista solo reacciona al estado del store | Pendiente |
| SCR-12-O1 | Pantallas | Impact Analysis — fetches solapados sin cancelacion | `getUpgradeImpact` (linea 96 de `outdated.tsx`) no se cancela cuando el cursor cambia rapido. El debounce de 150ms solo ayuda si las teclas paran ≥150ms. N fetches solapados posibles | Media | Condiciones de carrera en la actualizacion del panel de Impact Analysis; resultado de fetch anterior puede sobreescribir el resultado actual | `src/views/outdated.tsx:89-105` | Usar `AbortController` con cleanup en `useEffect`; cancelar el request previo antes de lanzar el nuevo | Pendiente |
| SCR-12-I1 | Pantallas | Hint `esc:cancel` enganoso en `InstalledView` | El hint `esc:{t('hint_cancel')}` se muestra durante `stream.isRunning` pero el `useInput` no llama `stream.cancel()` — el Esc captura la navegacion global, no cancela la operacion | Media | El usuario cree que puede cancelar la desinstalacion con Esc pero en realidad navega hacia atras | `src/views/installed.tsx:65-110,124` | Interceptar Esc durante `stream.isRunning` para llamar `stream.cancel()` antes de navegar | Pendiente |
| PERF-007 | Rendimiento | Impact Analysis — 2 spawns de `brew` por cursor | `useEffect([debouncedCursor, stream.isRunning])` dispara `getUpgradeImpact` en cada cursor move; para formulae ejecuta `brew deps --1 <pkg>` y `brew uses --installed <pkg>` (dos spawns). Sin cache entre cursor moves | Media | Navegando j/k en lista de 50 paquetes outdated: 100+ subprocesos `brew` con coste real (arrancar Ruby + cargar formula data). Degradacion de rendimiento perceptible | `src/views/outdated.tsx:89-105`; `src/lib/impact/impact-analyzer.ts:81,88` | Cachear resultados en `Map<string, UpgradeImpact>` por `(name, version)`; subir debounce a 400-500ms | Pendiente |
| PERF-011 | Rendimiento | `AppState.refresh()` serializa `brew update` antes del paralelismo | `await checker.updateIndex()` (timeout 120s) se ejecuta antes de `async let outdatedResult/servicesResult`. El tiempo a primer dato es `T(brew update) + max(outdated, services)` en lugar de `max(brew update, outdated, services)` | Media | La lista de outdated en BrewBar tarda el tiempo de `brew update` mas el tiempo de `outdated` en lugar de solaparlos | `menubar/BrewBar/Sources/Models/AppState.swift:51-55` | Lanzar `updateIndex()` como `async let` o `Task` no awaited; mostrar datos mientras se actualiza el indice | Pendiente |
| GOV-007 | Gobierno | Repositorio URL mismatch en `package.json` | `package.json:17` apunta a `MoLinesGitHub/Brew-TUI.git`; el remote real es `MoLinesDesigns/Brew-TUI.git`. Afecta tambien a `bugs.url` y `homepage` | Media | URLs rotas en npm registry; los usuarios que hacen clic en el link del repositorio desde npm obtienen 404 | `package.json:17` — `"url": "git+https://github.com/MoLinesGitHub/Brew-TUI.git"` | Unificar el usuario GitHub y actualizar `package.json`, README, Formula y Cask | Pendiente |
| GOV-008 | Gobierno | `dist-standalone/brew-tui-bun` comprometido en git | Binario Mach-O arm64 de 65 MB trackeado en git. No gitignoreado. Sin documentacion en README. Ocupa espacio permanente en el historial git | Media | Historial git inflado 65 MB; artefacto sin procedencia verificable; no hay SHA256 publicado para verificar integridad | `git ls-files dist-standalone/brew-tui-bun` — archivo tracked | Agregar `dist-standalone/` a `.gitignore` y `.npmignore`. Ejecutar `git rm --cached dist-standalone/brew-tui-bun` | Pendiente |
| DS-002 | Design system | Contraste alto incompleto en BrewBar | Cinco usos de color en produccion sin rama `colorSchemeContrast == .increased`: `.yellow` en `PopoverView:92`, `.red` en `:136,146`, `.orange` en `:197,201`, `.orange` en `OutdatedListView:83` | Media | Usuarios con "Increase Contrast" activado en macOS pueden ver colores con contraste insuficiente | `PopoverView.swift:92,136,146,197,201`; `OutdatedListView.swift:83` | Sustituir con `colorSchemeContrast == .increased ? Color(...) : Color.xxx` o usar colores del sistema adaptables | Pendiente |
| DS-003 | Design system | `GRADIENTS` hex desvinculados de `COLORS` | `src/utils/gradient.tsx` usa valores hex crudos (`#FFD700`, `#FFA500`, `#B8860B`) que no referencian `COLORS.gold` ni otros tokens | Media | Cambios en tokens de color no se propagan a gradientes; inconsistencia visual si se actualiza la paleta | `src/utils/gradient.tsx:60-68` | Derivar `GRADIENTS` desde `COLORS.*` o mapear explicitamente | Pendiente |
| DS-004 | Design system | MenuBarIcon apariencias invertidas en `Contents.json` | `menubar_dark@1x.png` asignada a apariencia por defecto y `menubar_light@1x.png` a luminosity=dark — el icono se muestra invertido en cada modo | Media | El icono del menu bar aparece incorrecto en modo claro y oscuro de macOS | `Assets.xcassets/MenuBarIcon.imageset/Contents.json` | Intercambiar las asignaciones: `menubar_light@` para default, `menubar_dark@` para dark | Pendiente |
| GOV-009 | Gobierno | `tsup target: node18` vs `engines: >=22` | `tsup.config.ts:9` declara `target: 'node18'`; `package.json:45` declara `engines.node >=22`. El bundle puede incluir polyfills innecesarios | Baja | Polyfills innecesarios en el bundle; bundle ligermanente mayor de lo necesario | `tsup.config.ts:9`; `package.json:45` | Cambiar `target` a `'node22'` en `tsup.config.ts` | Pendiente |
| GOV-010 | Gobierno | `exportOptions.plist` case mismatch con `.gitignore` | `.gitignore:21` excluye `ExportOptions.plist` (mayuscula E) pero el archivo comprometido se llama `exportOptions.plist` (minuscula e). En Linux CI (case-sensitive) el archivo se rastrea | Baja | En Linux CI (GitHub Actions, ubuntu-latest) el archivo no queda excluido | `.gitignore:21` vs `menubar/exportOptions.plist` | Corregir la entrada `.gitignore` a `exportOptions.plist` (minuscula) o anadir ambas variantes | Pendiente |
| GOV-011 | Gobierno | `menubar/build/` no gitignoreado | `.gitignore` excluye `menubar/Derived/` y `menubar/DerivedData/` pero no `menubar/build/`. Un commit accidental incluiria el xcarchive y artifacts de build | Baja | Riesgo de commitear binarios y archives firmados accidentalmente | `.gitignore:14` — excluye `Derived/` y `DerivedData/` pero no `build/` | Anadir `menubar/build/` a `.gitignore` | Pendiente |
| GOV-012 | Gobierno | `xcarchive` en disco para version 0.6.0 | El xcarchive en `menubar/build/` es de version 0.6.0 (creado 2026-04-30). No fue regenerado tras el bump a 0.6.1 | Baja | El zip publicado en GitHub Releases v0.6.1 puede provenir de un archive de la version anterior | `menubar/build/BrewBar.xcarchive/Products/.../Info.plist:26` — `0.6.0` | Regenerar xcarchive y firmarlo despues de cada bump de version | Pendiente |
| ARQ-006 | Arquitectura | `CVE_CACHE_PATH` declarado pero nunca consumido en TS | `data-dir.ts` exporta `CVE_CACHE_PATH` pero ningun modulo TS lo importa. El inventario asumia cache compartida que no existe | Baja | Dead code exportado; la arquitectura real (cache en memoria en TS, cache en disco en Swift) no esta documentada | `src/lib/data-dir.ts` — exporta `CVE_CACHE_PATH`; grep en `src/` retorna solo la declaracion | Eliminar `CVE_CACHE_PATH` de `data-dir.ts` si el cache no se va a compartir; documentar la arquitectura de cache | Pendiente |
| ARQ-007 | Arquitectura | `getBuiltinAccountType` export muerto | Siempre retorna `null` segun comentario; zero callers en produccion. Dead code documentado para eliminacion | Baja | Dead code que aumenta la superficie de mantenimiento | `src/lib/license/license-manager.ts:13` | Eliminar la funcion y el comentario | Pendiente |
| ARQ-008 | Arquitectura | `import SwiftUI` innecesario en `AppState.swift` | `AppState.swift` importa SwiftUI pero no usa ninguna API de SwiftUI. Difumina el limite Models/Views | Baja | Acoplamiento innecesario entre capa de modelos y framework de UI | `menubar/BrewBar/Sources/Models/AppState.swift:2` | Sustituir por `import Observation` si es necesario | Pendiente |
| ARQ-009 | Arquitectura | `SchedulerService` acumula responsabilidades mixtas | Gestiona timer, permisos de notificacion, envio de notificaciones, CVE y sync. 260 lineas con 5 responsabilidades | Baja | Cohesion debil; dificil de testear y modificar en aislamiento | `menubar/BrewBar/Sources/Services/SchedulerService.swift` — 260 lineas | Extraer `NotificationSender` con los metodos de envio de notificaciones | Pendiente |
| ARQ-010 | Arquitectura | Fire-and-forget tasks en `SchedulerService` sin handle de cancelacion | `Task { await syncNotificationPermission() }` lanzado en `start()` sin almacenar handle; no se puede cancelar si el servicio se detiene antes de que terminen | Baja | Tasks pendientes que no se cancelan al detener el scheduler | `menubar/BrewBar/Sources/Services/SchedulerService.swift:80,192` | Almacenar handle en `private var permissionSyncTask: Task<Void, Never>?`; cancelar en `stop()` | Pendiente |
| ARQ-011 | Arquitectura | Module-level singletons en stores TS | `brew-store.ts` y `license-store.ts` tienen variables de modulo que persisten entre tests en la misma ejecucion de Vitest | Baja | Contaminacion entre tests cuando el modulo persiste en el mismo runner | `src/stores/brew-store.ts` — `fetchAllInFlight`, `brewUpdateInFlight`; `src/stores/license-store.ts` — `_revalidatingPromise`, `_revalidationInterval` | Inicializar estas variables dentro del estado de Zustand o limpiar en `afterEach` de los tests relevantes | Pendiente |
| UI-010 | UI | Logo ASCII en header con `key={i}` | `key={i}` (indice de array) para lineas estaticas del logo ASCII — antipatron aunque sin impacto funcional real | Baja | Antipatron de React; React puede advertir en modo estricto | `src/components/layout/header.tsx:93` | Sustituir por `key={"logo-" + i}` | Pendiente |
| UI-011 | UI | Tres `useInput` activos simultaneamente en `ProfilesView` | Tres handlers de `useInput` con condiciones de activacion diferentes en `profiles.tsx:54,73,85`; fragil y dificil de trazar | Baja | Interacciones inesperadas posibles entre los tres handlers | `src/views/profiles.tsx:54,73,85` | Consolidar en un unico `useInput` con bloque switch/if sobre `mode` | Pendiente |
| UI-012 | UI | `ProfilesView` dos `useEffect` de cleanup potencialmente competidores | Si la limpieza del generador y el cierre del modal ocurren en orden incorrecto al desmontar, el modal podria no cerrarse | Baja | Modal puede quedar abierto al desmontar en condiciones de race | `src/views/profiles.tsx:37-52` | Consolidar en un unico `useEffect` que gestione ciclo de vida del generador y el modal | Pendiente |
| UI-013 | UI | `PackageInfoView` — conversion de CaskInfo en vista | La conversion `CaskInfo`→`Formula`-shape (lineas 52-93) es logica de adaptacion de modelo incrustada en la vista | Baja | Logica de negocio en capa de presentacion; viola separacion de capas | `src/views/package-info.tsx:52-93` | Extraer a `formulaeFromCask(cask: CaskInfo): Formula` en `src/lib/brew-api.ts` | Pendiente |
| UI-014 | UI | `SecurityAuditView` sin advertencia de cobertura parcial | No hay mensaje visible sobre que el ecosistema Bitnami no cubre todos los paquetes Homebrew; posibles falsos negativos sin advertencia | Baja | Usuarios Pro pueden creer que la ausencia de CVEs significa total seguridad | `src/views/security-audit.tsx:83-185` | Anadir nota `t('security_coverage_warning')` debajo del resumen estadistico | Pendiente |
| UI-015 | UI | `BrewBar PopoverView` `minHeight: 420` fijo | `.frame(minHeight: 420)` puede clipar contenido cuando el texto es mas grande con Dynamic Type activo | Baja | Contenido clipado para usuarios con fuente grande en macOS | `menubar/BrewBar/Sources/Views/PopoverView.swift:40` | Cambiar a `minHeight: 0` y dejar que el contenido determine la altura minima | Pendiente |
| DS-005 | Design system | Tema oscuro exclusivo en TUI | Tokens solo calibrados para fondo oscuro; `colors.ts:7-9` documenta la limitacion. Terminales claros muestran colores practicamente ilegibles | Media | Ilegibilidad en terminales con fondo claro; afecta a usuarios de macOS Terminal default | `src/utils/colors.ts:7-9` | Roadmap: detectar `COLORFGBG` o `TERM_PROGRAM` para variante de tema claro | Pendiente |
| UX-008 | UX | Onboarding de BrewBar incompleto | NSAlert de licencia no encontrada no ofrece URL de compra. Modo degradado no tiene CTA de renovacion en el popover | Baja | Usuarios sin Pro no saben donde comprar desde BrewBar; churn evitable | `menubar/BrewBar/Sources/Views/PopoverView.swift` — solo icono de candado + texto en modo degradado | Agregar boton "Renovar Pro" con URL de checkout en el popover de modo degradado | Pendiente |
| UX-009 | UX | Tecla `d` de desactivacion invisible para Team users | El hint de tecla `d` para desactivar solo se muestra si `status === 'pro'`; Team users no lo ven aunque pueden desactivar | Baja | Team users no descubren la opcion de desactivacion | `src/views/account.tsx` — condicion `status === 'pro'` en hint | Incluir `status === 'team'` en la condicion del hint | Pendiente |
| UX-010 | UX | Clave `account_validating` nunca renderizada | La clave `account_validating` existe en `en.ts` y `es.ts` pero el early return en `AccountView` usa `account_loading`; la otra clave es dead code | Baja | Dead code de traduccion; potencial confusion en mantenimiento de strings | `src/views/account.tsx` — early return usa `account_loading`, clave `account_validating` muerta | Eliminar la clave muerta o conectarla correctamente | Pendiente |
| PERF-002 | Rendimiento | Polling de 100ms en `streamBrew` | `await new Promise((r) => setTimeout(r, 100))` dentro del bucle del generador. Hasta 100ms de latencia por linea visible; despierta el event loop 10 veces por segundo | Baja | Latencia perceptible en operaciones de install/upgrade; uso de CPU innecesario durante streaming | `src/lib/brew-cli.ts:87` | Reemplazar con patron `Promise` resuelta desde `proc.stdout.on('data', ...)` | Pendiente |
| PERF-004 | Rendimiento | `DashboardView` y `OutdatedView` desestructuran el store completo | Ambas vistas hacen destructuring completo del store; cualquier mutacion del map `loading` o `errors` re-renderiza la vista entera | Baja | Re-renders innecesarios durante cargas de datos; contrasta con `installed.tsx` que usa selectores granulares | `src/views/dashboard.tsx:82`; `src/views/outdated.tsx:60` | Aplicar selectores granulares o `useShallow` | Pendiente |
| PERF-006 | Rendimiento | `useStdout()` invocado tras returns tempranos en `history.tsx` | Viola las reglas de hooks de React (ejecucion condicional). React 19 no siempre advierte pero un cambio en el orden producira warnings en strict mode | Baja | Violacion de rules of hooks; puede producir comportamiento indefinido si el orden de hooks cambia | `src/views/history.tsx:101-104` | Mover `useStdout()` al inicio del componente, antes de los returns tempranos | Pendiente |
| PERF-014 | Rendimiento | `readDataToEndOfFile()` sincrono en `terminationHandler` | `BrewProcess.swift:99` — ya catalogado como Alta en BK-002. Referencia cruzada de performance | Alta | Ver BK-002 | `menubar/BrewBar/Sources/Services/BrewProcess.swift:99` | Ver BK-002 | Pendiente |
| QA-009 | Testing | `brewbar-installer.ts` — camino feliz sin test | 9 tests cubren errores y edge cases, pero no el flujo exitoso de descarga + SHA-256 match + mv a /Applications | Media | El happy path del instalador de BrewBar no esta verificado | `src/lib/brewbar-installer.test.ts` | Agregar test con SHA-256 correcto mockeando `fetch` y `fs.promises` | Pendiente |
| QA-010 | Testing | Tests de persistencia — solo mocks de fs | Todos los tests de `history-store`, `profile-store`, `state-snapshot` usan `vi.mock('node:fs/promises')` sin tests contra el sistema de archivos real | Media | Bugs en la interaccion real con el filesystem pueden escapar a los tests | `src/lib/history/`, `src/lib/profiles/`, `src/lib/state-snapshot/` — tests con mocks puros | Agregar al menos un test de integracion por subsistema que escriba en `os.tmpdir()` | Pendiente |
| DS-006 | Design system | Colores locales no centralizados en BrewBar | `OutdatedListView.swift:10-11` define `installedVersionColor = Color.orange` y `currentVersionColor = Color.cyan` como constantes locales duplicadas | Baja | Inconsistencia visual si se actualiza la paleta; colores no auditables desde un punto central | `menubar/BrewBar/Sources/Views/OutdatedListView.swift:10-11` | Mover a `Theme.swift` con tokens reutilizables | Pendiente |
| DS-007 | Design system | `StatCard` default `color='white'` con literal en lugar de `COLORS.white` | `src/components/common/stat-card.tsx` prop `color = 'white'` usa literal en lugar de token | Baja | Inconsistencia semantica menor; si `COLORS.white` cambia, el default no se actualiza | `src/components/common/stat-card.tsx` | Cambiar a `color = COLORS.white` | Pendiente |
| ACC-002 | Accesibilidad | Fila de paquete en BrewBar sin agrupacion VoiceOver | `OutdatedListView.swift` — `HStack` con nombre, version y boton navegables independientemente por VoiceOver en lugar de como unidad | Baja | VoiceOver lee nombre, version y boton por separado en lugar de como una fila cohesiva | `menubar/BrewBar/Sources/Views/OutdatedListView.swift` | Agregar `.accessibilityElement(children: .combine)` en la `HStack` exterior | Pendiente |
| ACC-003 | Accesibilidad | Toggle de notificaciones sin `accessibilityLabel` explicito | `SettingsView.swift` — `Toggle("Enable notifications", ...)` depende de la inferencia de SwiftUI que es fragil | Baja | En algunos contextos VoiceOver puede no leer el label correctamente | `menubar/BrewBar/Sources/Views/SettingsView.swift` | Agregar `.accessibilityLabel(String(localized: "Enable notifications"))` explicito | Pendiente |
| ACC-004 | Accesibilidad | Deteccion de capacidades de terminal ausente | No se detectan capacidades de terminal (`$TERM`, `$COLORTERM`, `$TERM_PROGRAM`); la app asume soporte de color completo | Baja | En terminales sin soporte de color o con capacidades limitadas, la salida puede ser incorrecta | `src/app.tsx`; `src/utils/colors.ts` — sin revision de `COLORTERM` | Evaluar `process.env.COLORTERM === 'truecolor'` para habilitar gradientes; degradar en caso contrario | Pendiente |
| EP-001 | Endpoints | Double retry en deactivacion de licencia | `license-manager.ts:354-362` loop de 3 intentos; `apiDeactivate` ya usa `fetchWithRetry` (3 intentos) — hasta 9 solicitudes POST a `/deactivate` | Baja | Carga innecesaria sobre Polar API; posibles efectos secundarios si Polar contabiliza los reintentos | `src/lib/license/license-manager.ts:354-362` | Eliminar el loop externo o pasar `retry: { attempts: 1 }` a `fetchWithRetry` dentro de `apiDeactivate` | Pendiente |
| EP-002 | Endpoints | Promo `/redeem` sin idempotency-key | `promo.ts:142-172` — el servidor no recibe ningun token de idempotencia; un retry tras timeout puede doble-canjear un codigo promocional | Media | Un usuario puede canjear el mismo codigo dos veces si la conexion falla tras el envio | `src/lib/license/promo.ts:142-172` | Agregar `idempotencyKey: randomUUID()` al body; el servidor debe deduplicar por esa clave | Pendiente |
| GOV-013 | Gobierno | `.gitattributes` ausente | Sin `.gitattributes`, no hay normalizacion de line endings, ni marcadores de binary para PNG, ni `export-ignore` para tarballs de GitHub | Baja | Line endings inconsistentes entre plataformas; tarballs incluyen archivos de desarrollo innecesarios | Ausencia de `.gitattributes` en el repositorio | Crear `.gitattributes` con `* text=auto`, binarios marcados como `binary`, y `export-ignore` para `scripts/`, `superaudit-report/`, `screenshots/` | Pendiente |
| QA-011 | Testing | Sin snapshot testing | Sin infraestructura de snapshot testing en TS ni en Swift. Cambios visuales en vistas SwiftUI y componentes TUI son imposibles de detectar automaticamente | Media | Regresiones visuales en componentes de BrewBar o TUI pasan desapercibidas | `package.json` — sin `@vitest/snapshot` equivalente; `Project.swift` — sin `swift-snapshot-testing` | Para BrewBar: integrar `swift-snapshot-testing`; para TUI: usar `lastFrame()` de `ink-testing-library` como snapshot textual | Pendiente |
| PERF-005 | Rendimiento | `allOutdated` recompuesto en cada render sin `useMemo` | `src/views/outdated.tsx:82-85` recrea el array combinado de formulae y casks en cada render | Baja | Recrea un array y recalcula `visible` en cada cursor move aunque `outdated` no cambio | `src/views/outdated.tsx:82-85` | Envolver con `useMemo` dependiente de `outdated.formulae` y `outdated.casks` | Pendiente |
| PERF-015 | Rendimiento | `SyncMonitor` parsea `sync.json` dos veces por tick | `checkForSyncActivity()` y `getKnownMachineCount()` hacen cada una `Data(contentsOf:) + JSONSerialization` del mismo archivo | Baja | Dos lecturas y dos parseos innecesarios del mismo archivo en cada tick del scheduler | `menubar/BrewBar/Sources/Services/SyncMonitor.swift:23,42` | Refactor: un unico metodo `checkSync() -> (Bool, Int)` que parsea una sola vez | Pendiente |

# 21. Registro central de hallazgos

> Auditor: report-consolidator | Fecha: 2026-04-23

## Resumen

* **Total hallazgos:** 86
* **Criticos:** 4
* **Altos:** 21
* **Medios:** 36
* **Bajos:** 25
* **Reportes analizados:** 14 de 14
* **Reportes faltantes:** Ninguno

---

## Registro completo

| ID | Dominio | Subzona | Hallazgo | Severidad | Impacto | Evidencia | Accion | Estado |
|----|---------|---------|----------|-----------|---------|-----------|--------|--------|
| QA-001 | Testing | Calidad de suite | `npm run test` pasa en vacio con exit 0 — el gate de CI es un false-green que no bloquea releases aunque no exista ni un test | Critica | Un release puede publicarse sin ningun test ejecutado, ocultando regresiones completas | `.github/workflows/release.yml:25`; `npm run test` retorna 0 sin tests | Configurar vitest con `--passWithNoTests false` o bloquear el release hasta alcanzar cobertura minima | Pendiente |
| QA-002 | Testing | Unit tests | `getDegradationLevel` sin tests — funcion critica de time-bomb que controla el acceso offline Pro (rangos 0-7d, 7-14d, 14-30d, 30+d) | Critica | El modelo de negocio freemium puede comportarse de forma incorrecta en produccion sin que ningun test lo detecte | `src/lib/license/license-manager.ts:179` | Cubrir todos los rangos de tiempo con tests de fecha inyectada | Pendiente |
| QA-003 | Testing | Integration | Flujo completo de licencia sin test end-to-end (activate → saveLicense → loadLicense → decrypt → revalidate) | Critica | Regresion silenciosa en cualquier paso del flujo de licencia afecta a todos los usuarios Pro sin deteccion | `src/lib/license/` — cobertura indirecta insuficiente | Crear test de integracion con filesystem temporal que cubra el ciclo completo | Pendiente |
| REL-001 | Release | Firma y notarizacion | BrewBar.app distribuido sin firma Developer ID ni notarizacion — macOS Gatekeeper bloquea el lanzamiento en todos los usuarios por defecto | Critica | El producto es inusable para todos los usuarios que descarguen BrewBar; el subcomando `brew-tui install-brewbar` instala un binario que el sistema rechaza ejecutar | `.github/workflows/release.yml:62-66`; sin `CODE_SIGN_IDENTITY`, sin `codesign`, sin `notarytool` | Obtener Apple Developer Program; agregar firma Developer ID y notarizacion con `xcrun notarytool submit --staple` en CI | Pendiente |
| SEG-001 | Seguridad | SHA-256 / distribucion | Verificacion SHA-256 de BrewBar.app es codigo muerto en produccion — el archivo `.sha256` nunca ha sido generado por el workflow; el instalador recibe un 404 que el `catch` descarta silenciosamente; la comparacion de hash jamas ha ejecutado | Alta | Cualquier binario sustituido en GitHub Releases se instala sin verificacion de integridad; un atacante man-in-the-middle podria distribuir malware | `.github/workflows/release.yml` no genera `.sha256`; `brewbar-installer.ts:63,73,78` | Agregar `shasum -a 256 BrewBar.app.zip > BrewBar.app.zip.sha256` en CI; subir ambos archivos; corregir el `catch` para relanzar cualquier error que no sea checksum mismatch | Pendiente |
| SEG-002 | Seguridad | Autenticacion / licencias | Clave AES-256-GCM embebida en codigo fuente TypeScript (`ENCRYPTION_SECRET`, `SCRYPT_SALT`) y en binario Swift (hex hardcodeado en `LicenseChecker.swift`) — cualquier usuario puede derivar la misma clave y fabricar archivos `license.json` validos | Alta | Bypass del modelo freemium por cualquier usuario que lea el bundle npm publicado o use `strings BrewBar`; abuso limitado por revalidacion periodica contra Polar.sh (24h) | `license-manager.ts:61-62`; `LicenseChecker.swift:47` | Documentar como riesgo conocido inherente al modelo client-side; incorporar UUID de maquina como componente de la clave para que no sea portable entre dispositivos | Pendiente |
| ARQ-001 | Arquitectura | Separacion de capas | Cinco modulos de `lib/` importan `useLicenseStore` directamente del store layer, invirtiendo la jerarquia de dependencias (lib conoce stores) | Alta | Acoplamiento circular entre capa de dominio y estado; impide testing unitario de cada modulo en aislamiento; riesgo de efectos secundarios inesperados al refactorizar | `history-logger.ts:5`, `audit-runner.ts:3`, `cleanup-analyzer.ts:6`, `profile-manager.ts:9`, `brewbar-installer.ts:9` | Refactorizar para recibir `{ license, status }` como parametros expliciticos; eliminar la importacion directa de stores en lib | Pendiente |
| DS-001 | Design System | Tokens de color | `COLORS.ts` creado como fix de v0.1.0 pero con 0 importaciones — 253 literales hex hardcodeados permanecen dispersos en 26 archivos sin cambio alguno | Alta | Imposible cambiar la paleta de colores del producto sin editar decenas de archivos; colores inconsistentes entre componentes; sin sistema de diseno funcional | `src/utils/colors.ts` — 0 importaciones; 253 ocurrencias hex en 26 archivos | Importar `COLORS` en todos los componentes y vistas; reemplazar hex inline por referencias a tokens | Pendiente |
| ACC-001 | Accesibilidad | BrewBar — botones icono | Cuatro botones solo-icono en BrewBar sin `.accessibilityLabel`: Refresh (`arrow.clockwise`), Settings (`gear`), Quit (`power`) en PopoverView, y upgrade por paquete (`arrow.up.circle`) en OutdatedListView — inaccesibles para VoiceOver y Voice Control | Alta | Usuarios con discapacidad visual o motora no pueden usar estas funciones clave de BrewBar | `PopoverView.swift:57-61,161-164,167-170`; `OutdatedListView.swift:74-79` | Agregar `.accessibilityLabel(String(localized: "Refresh/Settings/Quit"))` o usar `Label("...", systemImage: "...")` con `.labelStyle(.iconOnly)` | Pendiente |
| ACC-002 | Accesibilidad | BrewBar — Dynamic Type | Frame fijo `340x420` en PopoverView bloquea Dynamic Type; `.font(.system(size: 40))` es tamano absoluto que no escala con la configuracion del sistema | Alta | Texto truncado o invisible para usuarios con texto mas grande configurado en Accesibilidad del sistema | `PopoverView.swift:32` (frame fijo); `PopoverView.swift:100` (size 40 absoluto) | Cambiar a `.frame(width: 340, minHeight: 420)` o `.frame(width: 340)` con altura dinamica; reemplazar `size: 40` por `.font(.largeTitle)` | Pendiente |
| QA-004 | Testing | Unit tests | Rate limiting (`checkRateLimit`, `recordAttempt`) sin tests — 5 intentos fallidos deben provocar lockout de 15 minutos | Alta | El mecanismo de proteccion contra fuerza bruta en activaciones podria estar roto sin deteccion | `src/lib/license/license-manager.ts:29-58` | Verificar lockout tras MAX_ATTEMPTS y reset en caso de exito | Pendiente |
| QA-005 | Testing | Unit tests | `validateProfileName` sin tests — prevencion de path traversal sin cobertura | Alta | Una vulnerabilidad de path traversal en nombres de perfil podria quedar sin detectar | `src/lib/profiles/profile-manager.ts:25-35` | Cubrir: nombre vacio, longitud maxima, caracteres especiales, `../` traversal | Pendiente |
| QA-006 | Testing | Integration | AES-256-GCM round-trip sin test — cifrado/descifrado de licencia sin verificacion automatizada | Alta | Una regresion en el cifrado o descifrado romperia el acceso Pro de todos los usuarios sin deteccion | `src/lib/license/license-manager.ts:73-103` | Crear test que encripte y desencripte datos verificando integridad del GCM tag | Pendiente |
| QA-007 | Testing | Integration | `polar-api.ts` sin mock de red — ninguna llamada a Polar.sh tiene cobertura de test | Alta | Cambios en el contrato de Polar.sh o regresiones en el flujo de activacion pasan desapercibidos | `src/lib/license/polar-api.ts` | Usar `vi.mock` o MSW para simular respuestas: activacion exitosa, clave invalida, red caida | Pendiente |
| QA-008 | Testing | Integration | Fallback HTTP 400 de OSV one-by-one sin test — rama critica de recuperacion sin cobertura | Alta | El fallback podria estar roto, haciendo que el security audit falle completamente en entornos donde el batch es rechazado | `src/lib/security/osv-api.ts:72-77` | Test que simule HTTP 400 del batch y verifique que se activa `queryOneByOne` | Pendiente |
| QA-009 | Testing | UI tests | `ink-testing-library` instalada sin uso — libreria de tests de UI configurada pero sin ningun test escrito | Alta | Los componentes criticos (`ConfirmDialog`, `UpgradePrompt`, `AccountView`) no tienen cobertura de regresion de renderizado | `package.json devDependencies` — `ink-testing-library ^4.0.0` sin ningun test que la importe | Crear tests de renderizado para componentes criticos | Pendiente |
| QA-010 | Testing | UI tests | Flujo de activacion de licencia sin test de UI | Alta | Una regresion en la pantalla de activacion no es detectable automaticamente | `src/views/account.tsx` | Crear test con `ink-testing-library` que verifique estado inicial, activacion, estado Pro | Pendiente |
| QA-011 | Observabilidad | Logging | Sin logging estructurado en TypeScript — 22 `console.*` dispersas sin metadatos ni niveles formales | Alta | Imposible diagnosticar errores en produccion; fallos de red, de licencia o de brew son silenciosos o indistinguibles | 22 `console.*` en `src/`; ninguna con metadatos estructurados | Adoptar `pino` o `winston`; o como minimo definir un wrapper `logger.ts` con niveles y contexto | Pendiente |
| QA-012 | Observabilidad | Logging | Swift sin logging absolutamente — 0 llamadas a `Logger`/`OSLog`/`print` en los 12 archivos de Sources/ | Alta | Fallos silenciosos en BrewChecker, SchedulerService y LicenseChecker son indiagnosticables | 0 llamadas a `Logger`/`OSLog`/`print` en `menubar/BrewBar/Sources/` | Integrar `os.Logger` con subsistema `com.molinesdesigns.brewbar` | Pendiente |
| QA-013 | Observabilidad | Crash reporting | Sin crash reporting en TypeScript ni en BrewBar | Alta | Imposible conocer la tasa de crashes en produccion; los usuarios que experimentan un fallo no tienen canal de reporte automatico | Sin `@sentry/node` ni equivalente en `package.json`; sin Sentry Swift en `menubar/Project.swift` | Integrar `@sentry/node` en TUI y Sentry Swift en BrewBar; configurar carga de dSYM en CI | Pendiente |
| EP-001 | Endpoints | Polar — activacion | `res.json() as PolarActivation` sin validacion de tipos en runtime — `activation.id` puede ser `undefined` si Polar cambia el contrato | Alta | Se guardaria una licencia con `instanceId: undefined`, rompiendo todas las revalidaciones posteriores | `polar-api.ts:66`; `activateLicense:94` | Agregar `assertPolarActivation(obj)` que verifique que `id` y `license_key.status` son strings | Pendiente |
| EP-002 | Endpoints | Polar — validacion | `res.json() as PolarValidated` sin validacion de tipos — `res.status` puede ser `undefined`, forzando expirado silencioso | Alta | Usuarios Pro pierden acceso sin notificacion de error; el sistema los degrada incorrectamente | `polar-api.ts:66,114-115` | Agregar `assertPolarValidated(obj)` que verifique `id`, `status` y `customer` | Pendiente |
| EP-003 | Endpoints | OSV batch | `OsvBatchResponse` sin validacion de tipos — si `data.results` no es array, los resultados se pierden silenciosamente | Alta | Paquetes vulnerables no aparecen en la auditoria de seguridad sin ninguna indicacion de fallo | `osv-api.ts:79-83` | Verificar `Array.isArray(data.results)` y longitud antes de iterar | Pendiente |
| EP-004 | Endpoints | OSV one-by-one | `catch {}` vacio descarta errores HTTP 5xx y de red — los paquetes afectados aparecen como "sin vulnerabilidades" — falso negativo de seguridad | Alta | Un fallo del servidor OSV.dev durante el fallback produce resultados incorrectos que dan falsa sensacion de seguridad | `osv-api.ts:113-115` — `} catch { // Skip packages... }` | Capturar el error; solo omitir en HTTP 400; propagar errores 5xx o de red | Pendiente |
| EP-005 | Endpoints | Instalador BrewBar | Limite de 200 MB inefectivo si el servidor omite el header `Content-Length` — el stream se descarga sin limite real | Alta | Un binario malicioso de tamano ilimitado podria descargarse e instalarse | `brewbar-installer.ts:62-63` — `contentLength = Number(res.headers.get('content-length') ?? '0')` devuelve 0 | Implementar contador de bytes durante el pipeline que aborte si supera el limite | Pendiente |
| REL-002 | Release | CI/CD | `xcodebuild build` en lugar de `xcodebuild archive` en CI — no se genera `.xcarchive`; el `.app` se extrae del `DerivedData` sin `exportOptions.plist` | Alta | El proceso de build no sigue el workflow oficial de Apple para distribucion; el artefacto producido no es reproducible ni rastreable | `.github/workflows/release.yml:62-66` | Cambiar a `xcodebuild archive` seguido de `xcodebuild -exportArchive -exportOptionsPlist` | Pendiente |
| REL-003 | Release | Cobertura de tests | Cobertura de tests insuficiente para release — flujos criticos (activacion, install/uninstall, pro-guard) sin cobertura | Alta | Un release puede introducir regresiones en los flujos mas criticos del producto sin deteccion automatica | `npm test` ya ejecuta 3 archivos / 8 tests pero no cubre flows criticos | Ampliar cobertura con tests unitarios y de integracion para `license-manager.ts`, `feature-gate.ts`, `brewbar-installer.ts` | Pendiente |
| BK-001 | Backend | Autenticacion | Clave de cifrado embebida en codigo fuente: `ENCRYPTION_SECRET` y `SCRYPT_SALT` en `license-manager.ts`; clave derivada hex en `LicenseChecker.swift` | Alta | (Cross-ref con SEG-002) Bypass de licencias por cualquier usuario con acceso al bundle | `license-manager.ts:61-62`; `LicenseChecker.swift:47` | Ver SEG-002 — documentar riesgo conocido; incorporar UUID de maquina en la derivacion de clave | Pendiente |
| SCR-001 | Pantallas | SmartCleanupView | Deteccion de huerfanos basada solo en `brew leaves` puede incluir falsos positivos para herramientas del sistema no gestionadas por Homebrew — sin advertencia explicita al usuario | Critica | El usuario podria desinstalar herramientas del sistema esenciales creyendo que son huerfanos seguros; la operacion es irreversible sin backup | `src/views/smart-cleanup.tsx:55,99,128-129` | Agregar advertencia explicita y requerir confirmacion de dos pasos con descripcion del riesgo | Pendiente |
| SCR-002 | Pantallas | InstalledView | Cabeceras de columna `'Package'`, `'Version'`, `'Status'` hardcodeadas en ingles — incumplimiento de i18n | Alta | Los usuarios con idioma configurado en espanol ven cabeceras en ingles; inconsistencia con el sistema de i18n del resto del producto | `src/views/installed.tsx:187-189` | Agregar claves `installed_colPackage`, `installed_colVersion`, `installed_colStatus` a `en.ts`/`es.ts` y usar `t()` | Pendiente |
| SCR-003 | Pantallas | SearchView | Fallback de error hardcodeado en ingles: `'Search failed'` no usa `t()` | Alta | Mensaje de error en ingles para usuarios con locale espanol | `src/views/search.tsx:52` | Reemplazar con `t('search_failed')` y agregar clave a `en.ts`/`es.ts` | Pendiente |
| SCR-004 | Pantallas | ProfilesView | Error de `fetchProfiles()` no se muestra en modo `list`; `loadError` solo aparece en sub-modos de creacion/edicion | Alta | El usuario no recibe feedback cuando la carga inicial de perfiles falla — la vista aparece vacia sin explicacion | `src/views/profiles.tsx:116-117` | Agregar `{loadError && <ErrorMessage message={loadError} />}` en la rama de renderizado de lista | Pendiente |
| SCR-005 | Pantallas | ProfilesView | Importacion masiva desde perfil sin indicar el numero de paquetes a instalar antes de confirmar | Alta | El usuario no puede hacer una decision informada sobre una operacion que puede instalar decenas de paquetes | `src/views/profiles.tsx:66-70` | Mostrar resumen (N formulae, M casks) antes de iniciar la importacion | Pendiente |
| SCR-006 | Pantallas | HistoryView | Replay de `upgrade-all` desde historial advierte al usuario que puede actualizar mas paquetes de los originales | Alta | El usuario podria actualizar paquetes inesperados creyendo que esta repitiendo exactamente la operacion original | `src/views/history.tsx:144-146` | Mostrar en el dialogo de confirmacion que "upgrade-all" actualizara todos los desactualizados al momento de la reproduccion | Pendiente |
| SCR-007 | Pantallas | AccountView | Fallo en `deactivate()` es silencioso — el `try/catch` no tiene rama `catch` con feedback de error | Alta | El usuario no sabe si la desactivacion fallo; queda en un estado ambiguo sin saber si la licencia sigue activa | `src/views/account.tsx:41-49` | Agregar `catch(err)` con `setDeactivateError(...)` y mostrar el error al usuario | Pendiente |
| SCR-008 | Pantallas | PackageInfoView | `api.getFormulaInfo()` solo consulta formulas; si el usuario navega a un cask desde SearchView, el resultado sera "Not found" sin explicacion | Alta | El usuario ve un error confuso cuando navega al detalle de un cask — funcionalidad bloqueada silenciosamente | `src/views/package-info.tsx:48` | Detectar si el nombre es un cask y llamar al endpoint correcto | Pendiente |
| SCR-009 | Pantallas | Todas (15/15) | Sin instrumentacion analitica en ninguna vista | Alta | Imposible medir engagement, funnel de conversion free→Pro, ni tasas de error en produccion | 0 referencias a tracking en todos los archivos de vistas | Integrar solucion de telemetria ligera con opt-in explicito | Pendiente |
| ARQ-002 | Arquitectura | Concurrencia | `_revalidating` flag booleano no es verdaderamente atomico — dos microtasks podrian pasar el guard simultaneamente en el mismo tick | Media | Dos revalidaciones concurrentes de licencia podrian producir estado inconsistente en el store | `src/stores/license-store.ts:12,58,79` | Reemplazar el flag por una `Promise` pendiente que actue como mutex real | Pendiente |
| ARQ-003 | Arquitectura | Flujo de datos | `brewUpdate().catch(() => {})` descarta cualquier error de `brew update` silenciosamente — el usuario no recibe feedback si la actualizacion falla | Media | El usuario trabaja con indices de Homebrew desactualizados sin saberlo; errores de conectividad o permisos son invisibles | `src/stores/brew-store.ts:147` | Cambiar a `.catch((err) => { set({ errors: { ...errors, update: String(err) } }) })` | Pendiente |
| ARQ-004 | Arquitectura | Cache | `lastFetchedAt` existe pero nunca se usa para invalidacion ni para mostrar "ultima actualizacion hace X" en la UI | Media | El usuario no sabe si esta mirando datos de hace 2 minutos o 2 horas; posible stale state invisible | `src/stores/brew-store.ts:17,40,57` | Leer `lastFetchedAt` en `DashboardView` y vistas de lista para mostrar timestamp | Pendiente |
| ARQ-005 | Arquitectura | Cache | Sin cache de resultados de OSV.dev — cada navegacion a la vista de seguridad re-consulta OSV completamente | Media | Latencia elevada y consumo innecesario de API en cada visita a Security Audit | `src/stores/security-store.ts` | Introducir cache en memoria con TTL de 15-60 min; solo refrescar si el usuario lo pide explicitamente | Pendiente |
| ARQ-006 | Arquitectura | Concurrencia | Tasks fire-and-forget en botones SwiftUI de BrewBar sin handle ni cancelacion | Media | Si el popover se cierra durante una operacion de upgrade, la Task no tiene mecanismo de cancelacion; puede producir actualizaciones de estado en `AppState` tras desaparecer la vista | `PopoverView.swift:57,88`; `OutdatedListView.swift:26,75` | Almacenar Task handles en `@State` y cancelarlos en `.onDisappear` | Pendiente |
| ARQ-007 | Arquitectura | Concurrencia | `DispatchQueue.global()` mezclado con Swift concurrency en `BrewChecker`; `OnceGuard` es `@unchecked Sendable` con `NSLock` manual | Media | Mezcla de GCD y Swift concurrency es fragil; `@unchecked Sendable` requiere disciplina manual | `BrewChecker.swift:78,27` | Reemplazar timeout con `withTaskGroup` o `Task.sleep` en structured concurrency | Pendiente |
| ARQ-008 | Arquitectura | Capas | `outdated.tsx` importa `execBrew()` directamente — el resultado no pasa por ningun parser ni se registra en el store o historial | Media | La operacion de pin/unpin es invisible al store y al historial Pro; rompe la abstraccion de la capa de API | `src/views/outdated.tsx:5` | Crear `pinPackage(name)`/`unpinPackage(name)` en `brew-api.ts` | Pendiente |
| BK-002 | Backend | Persistencia | `saveProfile` no es atomica — usa `writeFile` directo sin patron tmp+rename, a diferencia de `saveLicense` y `saveHistory` | Media | Un corte de energia o proceso terminado durante el guardado puede corromper el archivo de perfil | `profile-manager.ts:78` | Reemplazar por el patron `writeFile(tmpPath) + rename(tmpPath, finalPath)` | Pendiente |
| BK-003 | Backend | Validacion | Cast sin validacion de tipos en `loadLicense` — `JSON.parse(...) as LicenseData` sin type guard — campos pueden ser `undefined` donde se espera `string` | Media | Un archivo `license.json` modificado manualmente o corrompido puede producir comportamientos inesperados o crashes | `license-manager.ts:103` | Agregar `isLicenseData(obj): obj is LicenseData` con verificacion de campos antes del cast | Pendiente |
| BK-004 | Backend | Sincronizacion | Sin proteccion contra escrituras concurrentes entre procesos para `history.json` y `profiles/` | Media | Dos instancias de brew-tui ejecutandose simultaneamente pueden corromper datos de historial o perfiles | `history-logger.ts`, `profile-manager.ts` — sin lock de archivo | Agregar lock de archivo (`proper-lockfile` o equivalente) antes de `loadHistory + saveHistory` | Pendiente |
| BK-005 | Backend | Persistencia | Licencia almacenada en `~/.brew-tui/license.json` — no se usa el Keychain de macOS para el dato mas sensible | Media | Si el directorio `~/.brew-tui/` tiene permisos incorrectos por alguna razon, la licencia queda expuesta | Sin referencia a `SecItem` o `KeychainAccess` en el proyecto | Considerar almacenar la license key en Keychain y usar el archivo cifrado solo para metadatos | Pendiente |
| SEG-003 | Seguridad | Privacidad | Watermark invisible con email del usuario embebido en perfiles exportados via caracteres Unicode de ancho cero (U+200B/U+200C/U+200D) sin consentimiento explicito | Media | Violacion de privacidad — el email del usuario se transmite a terceros que reciben el perfil sin su conocimiento | `watermark.ts:25-38`; `profile-manager.ts:111` — comentario interno reconoce el problema | Notificar al usuario antes de exportar que el perfil contendra su email; ofrecer exportacion sin watermark | Pendiente |
| DS-002 | Design System | Componentes | Patron de banner exito/error duplicado 14+ veces (`Box borderStyle="round" borderColor="#22C55E/#EF4444"`) sin componente `ResultBanner` | Media | Inconsistencias visuales entre vistas; mantenimiento costoso al tener que editar 14+ copias del mismo patron | `installed.tsx`, `search.tsx`, `outdated.tsx`, `smart-cleanup.tsx`, `profiles.tsx`, `package-info.tsx`, `security-audit.tsx`, `doctor.tsx` | Extraer `<ResultBanner status="success|error" message={...} />` | Pendiente |
| DS-003 | Design System | Tokens | Sin tokens de espaciado — 114 ocurrencias de magic numbers en `paddingX/Y`, `gap`, `marginTop/Bottom` en 21 archivos | Media | Inconsistencias de espaciado entre componentes sin posibilidad de ajuste global | 114 ocurrencias en 21 archivos | Crear `src/utils/spacing.ts` con escala tokenizada | Pendiente |
| DS-004 | Design System | Componentes | Patron de fila cursor+texto sin componente `SelectableRow` — repetido en 6 vistas con colores hardcodeados | Media | Cursor y colores de seleccion inconsistentes entre vistas; duplicacion de codigo | Patron `<Text color={isCurrent ? '#22C55E' : '#9CA3AF'}>{isCurrent ? '▶' : ' '}</Text>` repetido en 6 vistas | Extraer `<SelectableRow isCurrent={bool} label={string} />` | Pendiente |
| DS-005 | Design System | Calidad visual | Contraste insuficiente: `#6B7280` (gris) sobre fondo negro del terminal da ratio ~3.5:1, por debajo del 4.5:1 de WCAG AA | Media | Texto secundario y pistas de teclado ilegibles para usuarios con baja vision o en condiciones de iluminacion adversas | Usado extensivamente en texto secundario y hints; ratio medido ~3.5:1 | Reemplazar por un gris con ratio >= 4.5:1 o proporcionar opcion de alto contraste | Pendiente |
| ACC-003 | Accesibilidad | BrewBar — adaptaciones | Sin soporte a Bold Text en BrewBar — sin uso de `@Environment(\.legibilityWeight)` | Media | Usuarios con Bold Text activado en Accesibilidad del sistema no ven efecto en BrewBar | Sin uso de `@Environment(\.legibilityWeight)` en los 3 archivos de vista Swift | Evaluar que textos deberian reforzar su peso con la configuracion del sistema | Pendiente |
| ACC-004 | Accesibilidad | BrewBar — adaptaciones | Sin soporte a Increase Contrast — sin uso de `@Environment(\.colorSchemeContrast)` | Media | Usuarios con alto contraste activado no obtienen beneficio en BrewBar | Sin uso de `@Environment(\.colorSchemeContrast)` | Agregar variantes de color de alto contraste para elementos de estado critico | Pendiente |
| ACC-005 | Accesibilidad | TUI — color | `VersionArrow` diferencia versions instalada/nueva solo por color (rojo vs teal) sin ningun otro indicador | Media | Usuarios con daltonismo no pueden distinguir la version instalada de la nueva | `src/components/common/version-arrow.tsx` — `current` en `#EF4444`, `latest` en `#2DD4BF`; sin etiqueta textual | Agregar etiquetas textuales ("instalado:", "disponible:") o simbolos contextuales | Pendiente |
| QA-014 | Testing | Unit tests | Parsers cubiertos solo parcialmente — faltan `parseInstalledJson`, `parseOutdatedJson`, `parseBrewConfig` | Media | Regresiones en el parsing de datos de brew no son detectadas; datos incorrectos llegan a la UI sin validacion | `src/lib/parsers/parsers.test.ts` — cubre `parseSearchResults`, `parseDoctorOutput`, `parseLeavesOutput`, `parseServicesJson` solo | Completar cobertura con fixtures reales de `brew info`, `brew outdated --json`, `brew config` | Pendiente |
| QA-015 | Testing | Unit tests | Funciones canary sin tests — `isProUnlocked()`, `hasProAccess()`, `isLicenseValid()` deben retornar siempre `false` en produccion | Media | Una regresion en las canary functions podria abrir el acceso Pro sin licencia | `src/lib/license/canary.ts` | Anadir test que verifique que las canary functions retornan `false` | Pendiente |
| QA-016 | Testing | Crash/diagnostic | Sourcemaps deshabilitados en produccion — sin mapas de pila para diagnostico de crashes reportados manualmente | Media | Cuando un usuario reporta un crash, el stack trace del bundle minificado es ininterpretable | `tsup.config.ts:13` — `sourcemap: false` | Habilitar sourcemaps en artifacts de release o mantenerlos en S3 privado para debugging | Pendiente |
| QA-017 | Testing | Crash/diagnostic | dSYM no preservado en CI — el paso de empaquetado solo incluye `BrewBar.app.zip` sin el archivo de simbolos | Media | Los crashes de BrewBar son imposibles de diagnosticar sin el dSYM correspondiente | `.github/workflows/release.yml:60-76` — sin referencia a dSYM | Modificar el paso de empaquetado para incluir el dSYM en el artifact o subirlo a un servicio de symbolication | Pendiente |
| QA-018 | Observabilidad | Analytics | Sin analytics en TUI ni en BrewBar — el funnel de conversion free→Pro es completamente invisible | Media | Imposible medir retencion, conversion, ni uso de features; el modelo freemium opera a ciegas | 0 referencias a cualquier SDK de analytics en ambas codebases | Integrar analytics con privacidad (PostHog self-hosted o Mixpanel) con opt-in explicito | Pendiente |
| EP-006 | Endpoints | Polar — validacion | `catch` en `revalidate()` agrupa errores de red y errores de contrato bajo el mismo flujo de grace — bugs de integracion quedan ocultos | Media | Un cambio de contrato en Polar.sh que produce `undefined` se trata como error de red y activa el grace period en lugar de reportar el fallo | `license-manager.ts:261` — `} catch { return isWithinGracePeriod... }` | Separar errores de red (tratar con grace) de errores de validacion (loguear y tratar como `expired`) | Pendiente |
| EP-007 | Endpoints | OSV batch | Versiones de paquete vacias o `undefined` incluidas en queries sin validacion — queries malformadas activan el fallback innecesariamente | Media | El fallback one-by-one es mas lento; versiones invalidas pueden producir resultados incorrectos | `osv-api.ts:129-132` — sin validacion de `p.version` | Filtrar paquetes con `version` vacia o nula antes de construir las queries | Pendiente |
| EP-008 | Endpoints | OSV one-by-one | Loop secuencial sin delay entre peticiones individuales puede exceder rate limits de OSV.dev | Media | Si OSV.dev aplica rate limiting, los escaneos de seguridad fallarian sin explicacion clara | `osv-api.ts:106-118` — `for (const pkg of packages)` con `await queryBatch(...)` sin delay | Agregar delay de 50-100ms entre peticiones y manejo de HTTP 429 con backoff | Pendiente |
| EP-009 | Endpoints | Instalador | Non-null assertion `split(/\s+/)[0]!` puede producir `"undefined"` como expected hash — la comparacion siempre falla | Media | La verificacion SHA-256 falla por error de parsing en lugar de por hash incorrecto; el fallo se descarta silenciosamente | `brewbar-installer.ts:75` | Validar que el resultado del split sea 64 caracteres hex antes de usarlo | Pendiente |
| EP-010 | Endpoints | Instalador | Sin validacion del formato del hash extraido — un archivo checksum malformado o interceptado con hash arbitrario podria pasar la comparacion | Media | Un atacante que controle el archivo `.sha256` podria sustituir el binario sin que la verificacion lo detecte | `brewbar-installer.ts:75-78` | Agregar validacion de formato SHA-256 (`/^[0-9a-f]{64}$/i`) del expected hash antes de comparar | Pendiente |
| EP-011 | Endpoints | brew CLI | `name` en `getFormulaInfo`, `uninstallPackage`, `serviceAction`, `installPackage`, `upgradePackage` pasa a `spawn` sin validacion — un nombre como `--force` seria interpretado como flag de brew | Media | Comportamiento inesperado de las operaciones brew si un nombre de paquete contiene caracteres de flag; potencial escalacion de permisos | `brew-api.ts:35-36,80-81,85-87,69,73` | Agregar validacion con `PKG_PATTERN` (`/^[\w@./+-]+$/`) antes de pasar a `spawn` | Pendiente |
| EP-012 | Endpoints | brew CLI | Sin timeout en `execBrew` y `streamBrew` — un proceso brew colgado bloquea la TUI indefinidamente | Media | La TUI queda irresponsiva sin posibilidad de cancelacion automatica; el usuario debe matar el proceso manualmente | `brew-cli.ts:3-21` (execBrew), `brew-cli.ts:23-79` (streamBrew) — sin `timeout` ni AbortController | Agregar `timeout: 30000` en `execFileAsync`; agregar timeout de 5min en streamBrew | Pendiente |
| SCR-010 | Pantallas | Todas TUI (12/12) | Sin manejo de escenario offline en TUI — errores de red se muestran como mensajes CLI crudos sin diferenciar "sin red" | Media | El usuario no puede distinguir un error de conectividad de un error de configuracion de brew | `brew-cli.ts` — sin pattern matching en stderr para errores de red | Agregar deteccion de error de conectividad y propagar tipo de error al store | Pendiente |
| SCR-011 | Pantallas | Todas TUI (12/12) | Colores hexadecimales hardcodeados en todas las vistas TUI — `#F9FAFB` y colores claros son ilegibles en terminales con fondo blanco | Media | El producto es ilegible para usuarios que usan terminales con tema claro | `#F9FAFB` en `dashboard.tsx`, `installed.tsx`, `history.tsx`, `account.tsx` (entre otros) | Definir paleta con variantes oscuro/claro; detectar esquema via `COLORFGBG` o flag `--theme` | Pendiente |
| SCR-012 | Pantallas | OutdatedView | Upgrade masivo (`brew upgrade`) confirmado con dialogo generico sin mostrar la lista de paquetes que seran afectados | Media | El usuario puede actualizar paquetes que no queria actualizar sin tener informacion previa | `src/views/outdated.tsx:61,121-123` | Mostrar lista completa de paquetes a actualizar en el dialogo de confirmacion de "Upgrade All" | Pendiente |
| SCR-013 | Pantallas | DoctorView | Flash de estado vacio — `loading.doctor` no se pre-inicializa a `true` a diferencia de `installed`/`outdated` | Media | Un frame donde la vista muestra contenido vacio antes del spinner puede confundir al usuario | `src/stores/brew-store.ts:61`; `src/views/doctor.tsx:19` | Pre-inicializar `loading: { ..., doctor: true }` en el store | Pendiente |
| SCR-014 | Pantallas | ServicesView | Error de `service-action` se resetea en `finally` antes de que el usuario pueda leerlo — puede desaparecer en el siguiente fetch | Media | El usuario ve brevemente un error de servicio que desaparece antes de poder leerlo completamente | `src/views/services.tsx:57,47-48` | Mantener el error hasta que el usuario pulse una tecla o haga una nueva accion | Pendiente |
| SCR-015 | Pantallas | PopoverView | `.font(.system(size: 40))` en `upToDateView` de BrewBar es tamano fijo que no escala con Dynamic Type | Media | Ya cubierto parcialmente por ACC-002; hallazgo especifico de la pantalla PopoverView | `PopoverView.swift:106` | Reemplazar por `.font(.system(.largeTitle))` | Pendiente |
| SCR-016 | Pantallas | SettingsView BrewBar | Fallo en `SMAppService.register()/unregister()` revierte el toggle silenciosamente sin feedback de error | Media | El usuario no sabe que el toggle de "Launch at login" oscila y vuelve a su estado anterior porque la operacion fallo | `SettingsView.swift:55-63` | Agregar `@State var loginError: String?` y mostrar mensaje de error cuando no sea nil | Pendiente |
| SCR-017 | Pantallas | SecurityAuditView | Sin mensaje diferenciado para error de servicio externo OSV.dev vs error local | Media | El usuario no puede saber si el problema es de su sistema o del servicio externo | `src/views/security-audit.tsx:58-59` | Categorizar el error en `useSecurityStore.scan()` para mostrar mensaje apropiado | Pendiente |
| GOV-001 | Gobierno | Info.plist | `NSMainStoryboardFile = "Main"` heredado del template de Tuist — la app usa SwiftUI con `@NSApplicationDelegateAdaptor`, no storyboards; la clave es un remanente | Media | Ruido en el Info.plist generado; posible confusion para desarrolladores | `Project.swift:30-35` — bloque `.extendingDefault(with:)` no elimina la clave | Usar `.dictionary(entries:)` o agregar `"NSMainStoryboardFile": ""` para sobrescribir | Pendiente |
| GOV-002 | Gobierno | Versioning | `CFBundleShortVersionString` no definido en `Project.swift` — el bundle podria mostrar version `1.0` en lugar de `0.2.0` si Tuist no resuelve `MARKETING_VERSION` correctamente | Media | Los usuarios y el sistema ven una version incorrecta del bundle; dificulta el rastreo de releases | `Project.swift:30-35` — sin `CFBundleShortVersionString` ni `CFBundleVersion` | Agregar `"CFBundleShortVersionString": "$(MARKETING_VERSION)"` y `"CFBundleVersion": "$(CURRENT_PROJECT_VERSION)"` | Pendiente |
| GOV-003 | Gobierno | Build settings | `SWIFT_STRICT_CONCURRENCY` no declarado explicitamente en `Project.swift` — se usa el default del compilador Swift 6 sin documentar la intencion | Media | Un cambio en los defaults de Tuist o del toolchain podria cambiar el nivel de concurrencia sin que nadie lo note | `Project.swift` — no contiene `SWIFT_STRICT_CONCURRENCY`; Swift 6 default es `complete` | Agregar `"SWIFT_STRICT_CONCURRENCY": "complete"` en base settings de `Project.swift` | Pendiente |
| FE-001 | Frontend | Jerarquia de vistas | `ProfilesView` con 267 lineas y 7 modos inline (FSM) sin descomposicion en subcomponentes | Media | Archivo dificil de mantener; los modos no se pueden testear independientemente; alta carga cognitiva | `src/views/profiles.tsx` — FSM `mode: 'list' | 'detail' | 'create-name' | 'create-desc' | 'importing' | 'edit-name' | 'edit-desc'` | Extraer `<ProfileListMode>`, `<ProfileDetailMode>`, `<ProfileCreateFlow>`, `<ProfileEditFlow>` | Pendiente |
| FE-002 | Frontend | Calidad de codigo | `COLORS.ts` creado como fix pero nunca importado — 189 literales hex inline (cross-ref con DS-001) | Media | Ya cubierto por DS-001; hallazgo especifico del dominio frontend | `src/utils/colors.ts` — 0 importaciones; 12 archivos de vistas con literales hex directos | Ver DS-001 | Pendiente |
| QA-019 | Testing | UI tests | `BrewBar` sin target XCTest/XCUITest | Media | Las 3 vistas SwiftUI y los 3 servicios de BrewBar no tienen ninguna cobertura de test automatizado | `menubar/Project.swift` — 0 targets de test definidos | Anadir target de tests y al menos un smoke test de arranque | Pendiente |
| QA-020 | Testing | UI tests | `ConfirmDialog` sin test de aceptacion/rechazo — componente critico para acciones destructivas | Media | Una regresion en el dialogo de confirmacion podria permitir acciones destructivas sin confirmation | `src/components/common/confirm-dialog.tsx` | Testear que responde correctamente a `y`/`Y` (en) y `s`/`S` (es) | Pendiente |
| GOV-004 | Gobierno | Secretos | `POLAR_ORGANIZATION_ID` embebido en `polar-api.ts` | Baja | Identificador publico de organizacion expuesto en el repositorio; impacto de seguridad real bajo | `polar-api.ts:9` | Mover a constante de configuracion si se quiere evitar exposicion explicita | Pendiente |
| GOV-005 | Gobierno | Build settings | ESLint con cobertura minimalista — solo `no-unused-vars` activo | Baja | Potenciales bugs que TypeScript strict no cubre pueden pasar sin deteccion | `eslint.config.js` | Activar `@typescript-eslint/recommended` | Pendiente |
| GOV-006 | Gobierno | Privacy | `PrivacyInfo.xcprivacy` pendiente de verificacion post-`tuist generate` | Baja | La configuracion es correcta pero no verificable sin ejecutar Tuist | `Project.swift:37`: `resources: ["BrewBar/Resources/**"]` | Ejecutar `tuist generate` y confirmar inclusion en bundle | Pendiente |
| GOV-007 | Gobierno | Privacy | Razon `CA92.1` posiblemente incorrecta en `PrivacyInfo.xcprivacy` para `NSPrivacyAccessedAPICategoryUserDefaults` | Baja | La app no usa App Groups; la razon correcta seria `1C8F.1` ("app functionality") | `PrivacyInfo.xcprivacy` — `CA92.1` ("App Group access") | Cambiar razon a `1C8F.1` | Pendiente |
| ARQ-009 | Arquitectura | Deuda | `previousView` en navigation-store es redundante con `viewHistory[-1]` — pueden divergir | Baja | Posible inconsistencia de estado en la navegacion si se modifica el historial sin actualizar `previousView` | `src/stores/navigation-store.ts:6,30` | Eliminar `previousView` del state y derivarlo de `viewHistory` donde se necesite | Pendiente |
| ARQ-010 | Arquitectura | Deuda | Patron `loading/error` repetido en cada vista sin abstraccion — TODO en `doctor.tsx:9` lo reconoce | Baja | Duplicacion de codigo; inconsistencias potenciales entre vistas | Multiple views; `src/views/doctor.tsx:9` (TODO existente) | Crear `<AsyncView>` o hook `useViewState(key)` para encapsular el patron | Pendiente |
| ARQ-011 | Arquitectura | Deuda | `LemonSqueezyActivateResponse` y `LemonSqueezyValidateResponse` en `lib/license/types.ts` — DTOs de red del proveedor anterior mezclados con tipos de dominio | Baja | Confusion entre contratos externos y modelo interno; naming obsoleto (LemonSqueezy ya no es el proveedor) | `src/lib/license/types.ts:24-49` | Mover estos DTOs a `polar-api.ts` y renombrarlos para el proveedor actual | Pendiente |
| ARQ-012 | Arquitectura | Deuda | Sin reintentos en `scan()` de seguridad ni en fetches de brew — fallos transitorios requieren accion manual del usuario | Baja | UX degradada en conexiones inestables | `src/lib/security/osv-api.ts` — sin logica de reintento | Implementar retry con backoff exponencial (max 2-3 intentos) para llamadas a servicios externos | Pendiente |
| FE-003 | Frontend | Renderizado | Clave composite en `ProgressLog` puede causar parpadeo si dos lineas comparten el mismo prefijo de 30 caracteres | Baja | Parpadeo visual durante operaciones largas de install/upgrade | `src/components/common/progress-log.tsx` — `key={line.slice(0,30) + (lines.length - visible.length + i)}` | Usar indice global monotonicamente creciente como clave | Pendiente |
| FE-004 | Frontend | Renderizado | `key={j}` en lineas de warning en `DoctorView` — indice de array como clave | Baja | Inconsistencia con el patron del resto de la codebase; riesgo bajo ya que los datos son estaticos | `src/views/doctor.tsx:40` | Usar `key={warning.slice(0,20) + '-line-' + j}` por consistencia | Pendiente |
| FE-005 | Frontend | Ciclo de vida | `Task {}` anonimos en botones de BrewBar sin handle de cancelacion | Baja | Tareas huerfanas si el popover se cierra durante una operacion (cross-ref con ARQ-006) | `menubar/BrewBar/Sources/Views/OutdatedListView.swift` | Almacenar handle en `@State` y cancelar en `.onDisappear` | Pendiente |
| FE-006 | Frontend | Ciclo de vida | `.onAppear` en `DoctorView` sin cleanup del fetch async | Baja | Si la vista se desmonta mientras el fetch esta en curso, puede producirse un setState en componente desmontado | `src/views/doctor.tsx:13` | Aplicar patron `mountedRef` ya usado en `package-info.tsx` | Pendiente |
| FE-007 | Frontend | Calidad | Ausencia de previews/snapshots para vistas TUI | Baja | Sin validacion visual automatizada de las 12 vistas TUI | `src/views/` — ninguna vista con `#Preview` o snapshot | Documentar en README; considerar snapshots de texto con `ink-testing-library` | Pendiente |
| FE-008 | Frontend | Layout | BrewBar PopoverView frame fijo `340x420` puede truncar texto con Dynamic Type grande (cross-ref ACC-002) | Baja | Texto truncado con tallas de fuente muy grandes en Accesibilidad | `menubar/BrewBar/Sources/Views/PopoverView.swift` | Cambiar a `frame(minHeight: 320, maxHeight: 500)` o `ScrollView` wrapper | Pendiente |
| FE-009 | Frontend | Arquitectura | `app.tsx` mezcla inicializacion de licencia con routing — TODO en linea 5 | Baja | Legibilidad y testabilidad reducidas | `src/app.tsx:8` — `useEffect(()=>{ initLicense(); }, [])` y switch de routing en el mismo componente | Extraer `<LicenseInitializer>` y `<ViewRouter>` | Pendiente |
| SEG-004 | Seguridad | Privacy | `hostname()` del equipo transmitido a Polar.sh como campo `label` durante la activacion sin informar al usuario | Baja | El nombre del equipo macOS (que puede contener el nombre real del propietario) llega al servidor de Polar.sh; el usuario no es informado | `polar-api.ts:73` — `label: hostname()` | Reemplazar con UUID anonimo generado localmente o con el `instanceId` ya existente | Pendiente |
| SEG-005 | Seguridad | Supply chain | `softprops/action-gh-release@v2` sin pin por hash SHA — tag mutable expone el workflow | Baja | Si el repositorio del tercero es comprometido, el tag puede apuntar a codigo malicioso | `.github/workflows/release.yml:89` | Reemplazar con `softprops/action-gh-release@<SHA>` | Pendiente |
| SEG-006 | Seguridad | Integridad | `checkBundleIntegrity()` falla-abierta — retorna `true` si no puede leer el archivo (permisos denegados, etc.) | Baja | La proteccion de integridad del bundle se desactiva silenciosamente en errores de lectura | `src/lib/license/integrity.ts:37` — `return true` en el bloque catch | Cambiar a falla-cerrada con log de advertencia | Pendiente |
| SEG-007 | Seguridad | Privacy | Sin mecanismo de eliminacion de datos del usuario (`brew-tui delete-account`) | Baja | No existe flujo para eliminar `~/.brew-tui/`; requerido por App Store si se distribuyera ahi | Sin subcomando `delete-account` en `src/index.tsx` | Agregar `brew-tui delete-account` con confirmacion | Pendiente |
| QA-021 | Observabilidad | Logging | `console.error` en `brew-store.ts` gateado por `NODE_ENV !== 'production'` — errores de `fetchLeaves` son silenciosos en produccion | Media | Errores de fetch de datos de brew no aparecen en ningun log en produccion | `src/stores/brew-store.ts:121-123` | Loguear errores no criticos en produccion con referencia opaca | Pendiente |
| BK-006 | Backend | Persistencia | Sin migracion de schema implementada — comentario `// Future: add migration logic here` en los tres modulos de persistencia | Baja | Un cambio de schema en una version futura dejara los datos de usuarios v1 ilegibles sin migrar | `license-manager.ts:113`, `history-logger.ts:36`, `profile-manager.ts:65` | Disenar e implementar logica de migracion antes de cualquier cambio de schema | Pendiente |
| BK-007 | Backend | Persistencia | Sin restriccion de unicidad en perfiles por nombre — `saveProfile` sobreescribe silenciosamente un perfil existente | Baja | El usuario puede perder datos de un perfil existente por sobreescritura accidental | `profile-manager.ts:78` — sin verificacion de existencia previa | Agregar comprobacion de existencia y lanzar error descriptivo si el perfil ya existe | Pendiente |
| REL-003 | Release | Localizacion | Cuatro cadenas con `extractionState: stale` en el catalogo `Localizable.xcstrings` de BrewBar | Baja | El catalogo no esta sincronizado con el codigo fuente; las traducciones existen y son correctas | `"Open Brew-TUI"`, `"Retry"`, `"Service Errors"`, `"Upgrade All"` marcadas como `stale` | Ejecutar `tuist generate` para re-extraer cadenas y limpiar el estado stale | Pendiente |
| REL-004 | Release | Versioning | `CURRENT_PROJECT_VERSION` ausente en `Project.swift` — sin numero de build `CFBundleVersion` | Baja | Sin numero de build no es posible distinguir entre diferentes builds de la misma version | `menubar/Project.swift` — solo `MARKETING_VERSION` configurado | Agregar `"CURRENT_PROJECT_VERSION": "1"` en `Project.swift` | Pendiente |
| REL-005 | Release | Marketing | Sin screenshots ni assets de marketing automatizados | Baja | Limita la documentacion y el listing si el producto se expande | Sin screenshots en el repositorio ni automatizacion para generarlos | Agregar screenshots al README o carpeta `/assets/screenshots/` | Pendiente |
| ACC-006 | Accesibilidad | BrewBar — semantica | Imagenes decorativas en OutdatedListView sin `.accessibilityHidden(true)` | Baja | VoiceOver leera "arrow.right" como contenido significativo | `OutdatedListView.swift:56-58` — `Image(systemName: "arrow.right")` decorativa | Agregar `.accessibilityHidden(true)` | Pendiente |
| ACC-007 | Accesibilidad | BrewBar — semantica | Cabeceras de seccion sin `.accessibilityAddTraits(.isHeader)` en PopoverView y SettingsView | Baja | La estructura de la pagina no es accesible para usuarios de VoiceOver que navegan por cabeceras | `PopoverView.swift:43`; `SettingsView.swift:13` | Agregar `.accessibilityAddTraits(.isHeader)` | Pendiente |
| ACC-008 | Accesibilidad | BrewBar — adaptaciones | Sin proteccion para Reduce Motion en animaciones futuras | Baja | No hay animaciones actualmente, pero no hay patron establecido para protegerlas si se agregan | Codigo Swift sin `@Environment(\.accessibilityReduceMotion)` | Agregar wrapper de utilidad que verifique `.accessibilityReduceMotion` | Pendiente |
| QA-022 | Observabilidad | Metricas | Sin medicion de latencia de APIs externas | Baja | Imposible detectar degradacion en el rendimiento de Polar.sh o OSV.dev | Sin instrumentacion antes/despues de `fetchWithTimeout` | Anadir `Date.now()` antes/despues de cada llamada en modo debug | Pendiente |
| QA-023 | Observabilidad | Metricas | Fallos de SchedulerService silenciosos en produccion — errores se asignan a `self.error` (string en UI) sin persistencia | Baja | Los fallos del scheduler de BrewBar son invisibles mas alla de la sesion activa | `menubar/BrewBar/Sources/Models/AppState.swift:33-43` | Persistir el ultimo error en `UserDefaults` con timestamp | Pendiente |
| SCR-018 | Pantallas | DashboardView | Sin adaptacion al ancho del terminal — StatCards en fila pueden solaparse en terminales estrechas | Baja | Layout roto en terminales con menos de 60 columnas | `src/views/dashboard.tsx:49-62` | Importar `useStdout()` y adaptar el numero de StatCards por fila segun columnas disponibles | Pendiente |
| SCR-019 | Pantallas | AccountView | `AccountView` sin atajo de teclado numerico — solo accesible via Tab/Shift+Tab | Baja | Descubrimiento dificil de la vista de cuenta; el usuario que necesita activar la licencia puede no encontrar la vista | `src/stores/navigation-store.ts:14-17` | Documentar el atajo en el footer o agregar AccountView a VIEWS con numero de tecla | Pendiente |
| EP-013 | Endpoints | Instalador | Dos instancias de `install-brewbar` concurrentes comparten `/tmp/BrewBar.app.zip` — pueden corromperse mutuamente | Baja | Caso extremadamente improbable pero puede corromper el ZIP durante la instalacion | `brewbar-installer.ts:16` — `const TMP_ZIP = '/tmp/BrewBar.app.zip'` | Usar path temporal unico: `os.tmpdir() + '/BrewBar-' + randomUUID() + '.zip'` | Pendiente |
